# JUDGE rubric — GUI presentation & discoverability

The JUDGE method (acceptance.html §09) scores the subjective and emergent GUI
qualities that resist assertion. An agent drives the **real renderer** through an
injected `window.stint` mock (`packages/gui/judge/`), captures screenshots + the
accessibility tree, and scores each item below PASS/FAIL with a one-line
justification citing the screenshot. Deterministic sub-facts are asserted by the
harness directly and enumerated per row in **Sub-facts** below, which
`packages/gui/test/judge-bind.test.ts` binds to the names the harness records; the
genuinely subjective items are scored by an LLM/human over the captured screenshots. Any FAIL fails the suite; a sample of PASSes is spot-checked.

The harness writes `acceptance/evidence/judge-report.json` and the screenshots in
`acceptance/evidence/screenshots/`. Run it with `npm run judge`.

> Renderer windows run headless via the pre-installed Chromium (Electron's own
> binary is not fetched in this environment). The **tray icon's own title count-up**,
> a **real global-hotkey press**, and the **tray's left/right-click behavior**
> (single left-click → compact popover only; right-click → minimal Quit-only menu;
> the dropdown action menu is removed, §12 R01 / G8) have no host here and are
> confirmed under MANUAL on a real desktop session (acceptance.html §11, residual
> risk). JUDGE checks only the headless-provable half: that the popover carries every
> tray action (`TRAY_POPOVER_SURFACE`).

| Item | Claim (PASS when true) | PRD | Evidence |
|------|------------------------|-----|----------|
| `NAV_SHELL` | The main window presents a **persistent left-hand nav** with the five views — **Timer / Entries / Clients / Reports / Settings**, in that order — beside the routed view area. Exactly one item is active by default (**Entries**), and its view is the one shown; clicking a different nav item **routes** to that view (its section becomes the visible one) and **moves the active highlight** to it. **The active item is the design.html D12 lifted chip — selection ≠ accent:** a raised **paper** chip (computed background `--paper`), **ink** label, and a non-none **chip-lift shadow**, with the accent confined to its **icon** (`--accent` on `.ic` only); the four inactive items stay flat (no shadow, no accent icon). Machine-scored — an accent-weak active fill would fail every chip fact. Routing is client-side (no IPC). The rail reads as a quiet desktop shell, not a busy web page. **Two hardened G7 sub-facts (MODIFIED §12 R3):** `SIDEBAR_EVERY_VIEW` — routing to **each** of the five views keeps the `.shell .nav` rail visible (width>0, not hidden) in **all five**, with exactly one `.view` visible each time, so **no view escapes the shell**; and `FIXED_WIDTH_ON_RESIZE` — the rail's measured width is **one fixed width across viewports** (1040/1440/1920px; 168px is the JUDGE-pinned width) while the `.views` column width changes, proving the sidebar is a fixed-width rail on resize (resize lands on the content area). A regression (a view that hides the rail, a rail that shrinks/grows on resize, or an active item that turns accent) flips the pass to false. | §12 R3, R13 · design.html D12 | `main-nav.png`, `main-nav-wide.png` |
| `EMPTY_STATE` | The empty main window instructs rather than decorates — it names a concrete next action ("press Ctrl+Alt+T" **and** "run `tt start`"). | §12 R5 | `main-empty.png` |
| `KEYBOARD_FOCUS` | **This row carries the A04 floor (focus visible, WCAG 2.4.7/2.2).** Every interactive control in the window is **reachable by Tab in reading order** and shows a **visible focus ring when — and only because — it holds keyboard focus**: the ring predicate is a **computed-style DELTA** — the control's `outline`/`box-shadow` signature under `:focus-visible` must **differ from its own unfocused baseline** — so a persistent decoration (the D12 chip-lift shadow on a selected chip, a static border) can never fake a ring. The ring idiom is the ONE design.html D13/A04 name and A02 puts a 3:1 floor under: a full-strength `--accent` boundary on every focus stop — drawn as an `outline` so no component rule can outrank it — with a field adding the 3px `--ring` halo inside it. There is no second, neutral ring; `--rule-strong` clears no floor at 1.89:1 (issue #137). The token side is pinned deterministically by GOLD `packages/gui/test/design-guard.test.ts` (a declaration census over every focus rule on every surface, plus the A02 floors recomputed from `design.tokens.json`); this row scores the rendered result. The window is fully operable from the keyboard: a Tab-walk from `<body>` advances through every visible control with the active element **never trapped on `<body>` or null** (no focus void / trap). Driven on both the empty and the running main window. Machine-scored facts: the Tab-walk reaches every visible focusable control in each window, each focused control's ring signature differs from its unfocused baseline, and focus never sticks on `<body>`. | §12 R14, §14 · design.html A04/D13 | `main-focus.png` |
| `TRAY_COUNTUP` | The popover shows a single running timer counting up; between two captures the displayed elapsed increased by ~3s, not reset. (The tray icon's own title count-up is checked under MANUAL.) | §12 R1 | `popover-running-1.png`, `popover-running-2.png` |
| `TRAY_POPOVER_SURFACE` | The compact popover is the **SOLE tray action surface** — the **Stop/Start toggle + Open Stint** are reachable in the popover, with **no Switch affordance** (Switch is removed entirely — issue #34) and **no dropdown action menu** (the tray's single left-click opens this popover; the old 3-item Start/Stop + Open Stint dropdown is **removed**, G8). The tray's own click/right-click behavior has no host headless and is confirmed under **MANUAL** (a single left-click opens the popover only; a right-click yields at most a minimal Quit-only OS menu). Machine-scored facts (the headless-checkable half): driving the real popover renderer, the **running** snapshot exposes `#toggle` reading 'Stop' (`aria-pressed=true`) and `#open` ('Open Stint') present, with **no `#switch` element** in the popover (running OR idle); the **idle** snapshot has `#toggle` reading 'Start' and `#open` present — so every tray action lives in the popover (since the dropdown is gone, the popover must carry them all; fails the moment Stop/Start or Open Stint is absent, or any `#switch` element reappears — Start *is* the atomic stop-then-start, so no separate switch verb is needed). | §12 R01 | `popover-running.png`, `popover-tray-surface.png` |
| `POPOVER_REJECT` | **A refused Stop/Start from the tray popover is surfaced in the popover's own announced region, never a silent no-op (§12 R21 — the tray twin of `WRITE_REJECTION_FEEDBACK` site d).** Driving the real popover renderer over the running snapshot with the strict-rejecting mock (`rejectWrites` — the same rejecting `toggle` the main-window site d uses), clicking **Stop** rejects and the popover **catches and displays**: the `#pop-warning` region is **visible, non-empty, and announced** (`role=status` + `aria-live`), carrying the reason ALONE ('stop time is before the entry started') — the mock rejects in **Electron's real `ipcRenderer.invoke` shape** (the invoke wrapper plus the thrown class, as the QA driver reproduces it), so the region is scored on **what the user reads**: the kernel exactly, with no "Error invoking remote method", no "StoreError", no `--flag` name (issue 138). Crucially the popover **stays operable**: `#toggle` is still present, enabled, and still reads **'Stop'** (the popover never pretends the refused stop landed — the running face persists), **Open Stint** stays reachable, and a **second** Stop attempt rejects again with the warning still shown (repeatable, not one-shot). Machine-scored facts: the announced non-empty `#pop-warning` after the refusal; the live, still-'Stop' toggle on the still-`.running` popover with `#open` present; and the persisting warning + live toggle after a second attempt. Fails if the popover swallows the rejection (no visible/announced message), flips to idle as if the stop landed, or wedges its controls. | §12 R21, §12 R01 | `popover-reject.png` |
| `IN_WINDOW_TIMER` | The **full Active-Timer card lives in the Timer view**, while the **Entries view keeps only a compact strip** of it. In the **Timer view** (reached by clicking the Timer nav item) the full **Active-Timer card** mirrors `tt status`: a **live per-second count-up** (advancing +3s across a pinned-clock step, not reset), a **running state** indicator, the running entry's **description** + **client/project** label, and the primary **Stop** action (Stop is the view's `--accent-solid`-filled primary, design.html D11) — with **no Switch control** (Switch is removed, issue #34) and **no start affordance while running** (the start panel is idle-only, §12 R05 / issue #51 — asserted by `RUNNING_SINGLE_ACTION`); the card's accent is confined to the running clock + state indicator (mirroring the popover count) and the rest of the card chrome stays monochrome. On the **Entries view** the **compact `#timer-strip`** mirrors the running count-up + state + description and links to the Timer view, but carries **no full-panel Stop control**. Machine-scored facts: the Timer-view card is hosted inside the `data-view="timer"` section, its clock reads the derived count-up and advances +3s, its text carries the running description ('auth refactor') and client/project ('Client A / API'), and a Stop control is present and visible while running **with no `#timer-switch` element anywhere**; AND the Entries-view strip shows the running count-up ('01:24:07'), state ('running'), and description ('auth refactor') while exposing no `#timer-stop` (and no `#timer-switch`), its clock computing the design.html D06 compact Clock role — **24px, tabular numerals** (issue #141: the running count-up paints `--accent`, which carries text only on A01's ≥24px large-text branch). **Idle strip (STATES.md Entries × edge):** on an idle snapshot the Entries view **still paints the compact strip** in its idle face — `00:00:00` clock, state `idle`, empty description — never a vanished or stale-running strip. Would fail if the full panel stayed on Entries, the card/strip placement regressed, a `#timer-switch` reappeared, or the idle strip vanished/kept running data. | §12 R04, §12 R14 | `timer-view.png`, `main-timer.png`, `main-timer-idle.png` |
| `CROSS_VIEW_FRESHNESS` | The Active-Timer card **mirrors `tt status` even after an Entries-toolbar control has been touched** (issue #50 regression, §12 R04). The renderer latches an entries-query flag once a range/filter/search control is used; the pre-fix `load()` early-returned into the entries-only query and never repainted the shared surfaces, so on the Timer view a Start click mutated the DB while the card stayed frozen on its idle face. Machine-scored facts, driven over the idle list fixture: **(a)** clicking the Entries toolbar's **Today** range preset records a `listEntries` query (the toolbar latched); **(b)** routing to the Timer view paints the idle card (state `idle`, primary reads `Start`, clock `00:00:00`); **(c)** clicking **Start** flips the card to running **in place, without any reload** (a window marker set before the click survives): state text `running`, the card carries `.running`, and — per the idle-only start panel (§12 R05 / issue #51) — the whole `#start-panel` hides (its one-tap `#toggle` with it) while the accented `#timer-stop` primary becomes visible; and **(d)** the count-up **advances +3s across a pinned-clock step** (live, not a one-off stale paint). Would fail if the card stayed idle/frozen after the toolbar was touched — the exact #50 symptom. | §12 R04 | `timer-cross-view.png` |
| `TIMER_VIEW` | The **full Timer view (G5)** is the GUI core-entry surface, and the **running entry's start is adjusted through the inline START-ONLY picker disclosure (§05 R06 / §12 R14)**. Routing to the Timer view renders the **live clock** reading the **derived count-up** (now − startUtc − excludedSeconds) — it **advances +3s** across the pinned-clock step, **not reset**, and **keeps counting while the start is edited** (editing the open row never stops it) — with the **live-edit-running strip** present: a **raw text Start field** (`#le-start`, `type="text"` in localInputValue format — **no native `datetime-local`**, G1) and **no End-time input** anywhere. That value is **the string the user retypes**, not a serialization: it renders **space-separated, seconds always** (`2026-06-24 21:35:53`), matches **no `T`-separated pattern**, and its **`placeholder` describes that exact shape** (`YYYY-MM-DD HH:mm:ss` — before issue #159 the two agreed with neither each other nor the mockups). Both spellings still **parse**. Clicking the Start field's calendar affordance (`#le-start-pick`, `aria-expanded`) expands the picker **IN FLOW** into `#le-start-disc` below the field — **zero `.stp-backdrop` / dialog / modal chrome anywhere**, the container computes `position: static` — showing the **running block** with a **start drag grip ONLY** (`.stp-grip`; **no `.stp-resize` end grip, no end label, no end echo field** — the variant takes no end binding, so its write path is structurally incapable of producing an end) and the **computed future transparency fade** (`.stp-block.me.open` `mask-image` gradient) dissolving the block toward the future; the snapshot's other same-day entries paint **gray**. **Dragging the grip a known pixel delta advances the raw `#le-start` text LIVE by the snapped 5-min amount** (−30px on the 720px/24h track = −60min: the exact 21:35:53 → 20:35:00 — the field seeds the stored start EXACTLY, to the second (§12 R15 / issue #49); only the dragged grip snaps), and the **debounced commit sends an `edit` patch over IPC (`window.__EDITED__`) that carries `startUtc` but has NO `endUtc` key** — the open row stays open, its end **empty, never a synthetic "now"**. A visible **Stop** (the accent-solid-filled primary) is present while running, with **no `#timer-switch`** (issue #34). The card also **says** it is running (design.html **D05/A05**, issue #142): the word **`running`** is in the card's **rendered `innerText`** — painted by the state line, not merely held in a `display:none` node the way it was for as long as that bug shipped — and an **accent-filled dot** sits beside it, so the app's most important state never rides on the recoloured count-up alone. Machine-scored facts: the clock reads '01:24:07' and advances +3s, computing the design.html D06 full Clock role (**38px, tabular numerals, the `--num` stack**); the card's rendered `innerText` contains `running`, the `#timer-state` line is laid out and visible with that word as its text, and the `.tc-dot` beside it is visible with a computed background equal to the resolved `--accent` (the OUTCOME, replacing the presence-only `#timer-state.textContent` probe, which the shipped bug satisfied); the strip is present with no `#le-end` and a `type="text"` Start; the disclosure opens in flow (static position, no backdrop, no dialog role, toggle `aria-expanded=true`); the start grip is present with no end grip/label/echo; the running block's computed mask-image contains a gradient; ≥1 gray other entry paints; the Start field seeds the exact stored `2026-06-24 21:35:53`, carries the placeholder `YYYY-MM-DD HH:mm:ss`, **matches that placeholder's shape digit-for-digit**, and matches **no** `\d{4}-\d{2}-\d{2}T` pattern (issue #159), and the grip drag advances `#le-start` to `2026-06-24 20:35:00` live — still `T`-free; and the committed patch is exactly a `startUtc` ('2026-06-24T20:35:00.000Z') with **no `endUtc` key**. The card's **attribute row also keeps an attribute distinct from an advisory** (design.html **D04/D14**, issue #160): a second page over an open entry that is **billable AND slept through** paints both roles at once, and **`slept` takes the whole `--flag` warn triple** (text, fill and rule, on the 999px pill) while **`billable` — the normal, overwhelmingly common state of a tracked entry — stays the quiet `--muted` label**, with a transparent fill, no rule, no pill radius and **no part of that triple**; neither carries the accent, and both are still **worded**. Scored as a **pair**, because until #160 all three labels shipped through one `.flag` class and every running card wore the amber reserved for "look at this" — read one colour at a time, an amber `billable` is a perfectly good warn pill. Would fail if the running state went back to speaking in colour alone (the word hidden, the dot dropped or left unfilled), a modal/backdrop reappeared on this surface, an end affordance or value materialized, the future fade were absent, the drag stopped writing the text field live (or off-snap), the count-up reset, the patch ever carried `endUtc`, the field drifted back to a wire-shaped value its own placeholder does not describe, or `billable` reclaimed any part of the warn triple (or `slept` lost it). | §12 R14, §05 R06 | `timer-view-full.png`, `timer-card-attr-vs-flag.png` |
| `FUTURE_START_GUARD` | A **mistyped FUTURE start on the running entry is REFUSED and surfaced where it was typed**, never the silent wedge issue #61 caused ("Stop appears dead"). Driving the real renderer over the **future-start guard mock** (the `edit` IPC rejects a start landing **after now** exactly as core's `edit()` does, §05 R06 / §03), typing a **next-day instant** into the running Start field (`#le-start`) and letting the **debounced live-edit commit** fire raises the **announced** (`role=status` + `aria-live`) **`#timer-warning`** region — genuinely on-screen in the active Timer view — with the reason **and only the reason** ("start time is in the future" — the mock rejects in Electron's real wrapped shape, so a painted invoke wrapper / exception class / `--flag` fails the item, issue 138), and **records NOTHING** (`window.__EDITED__` stays null). Crucially **no wedge**: the **live-edit strip stays present** and **Stop is still there** (the count-up never froze). **Correcting** the start to a valid **past** instant then commits cleanly — the warning **clears** (via `load()` → `clearOverlapBanner()`) and `window.__EDITED__` carries the corrected `startUtc` with **no `endUtc` key** (the open row stays open) — and clicking **Stop** flips the status to **idle**: the timer was stoppable all along. Machine-scored facts: the future-start commit shows an announced, non-empty `#timer-warning` while `__EDITED__` stays null and the strip + Stop persist; the corrected commit clears the warning and records `startUtc` '2026-06-24T22:00:00.000Z' with no `endUtc`; and Stop flips `status.running` to false. Would fail if the refused live edit were silently swallowed (no visible warning), if the future start were recorded, if the strip/Stop vanished (a wedge), or if correcting-then-stopping did not work. Builds on the WRITE_REJECTION_FEEDBACK precedent (the same #65 `#timer-warning` region). | §05 R06, §05 R01, §03, §16, §12 R21 | `timer-future-start-reject.png` |
| `FAVORITES_RAIL` | The Timer view's **pinned favorites rail** renders **one row per `FavoriteView`** (name + client/project/billable meta), each with a **one-click Resume** that fires `window.stint.startFavorite({name})` **exactly once**, plus a **Pin-as-favorite** affordance that fires `window.stint.pinFavorite` and a **kebab** exposing **rename / unpin** (`renameFavorite` / `unpinFavorite`). The **empty-favorites state** instructs ('pin a favorite' / mentions `tt fav`). The rail chrome is **monochrome** (accent reserved for the primary action), and `window.stint` exposes a **callable method for each of the five favorite channels** (`listFavorites` / `pinFavorite` / `renameFavorite` / `unpinFavorite` / `startFavorite`) — the PARITY_REACH extension for favorites, so the rail is the reachable GUI surface for the `tt fav` family (§17 R14). Machine-scored facts: three seeded rows (Standup / Deep work / Admin) each with a Resume + kebab; a Pin affordance present; clicking the first Resume fires `startFavorite` exactly once with `{name:'Standup'}`; all five favorite channels callable on `window.stint`; **the pin, rename and unpin flows are DRIVEN to completion** (STATES.md Timer × edge; issue #52 — Electron's renderer implements no `window.prompt`): the inline pin field committed on Enter fires `pinFavorite` with the typed name + the open-row template and the new chip lands in the rail; kebab → Rename through the same inline field fires `renameFavorite` and the chip's name changes; kebab → **Unpin** fires `unpinFavorite` **exactly once** with the chip's ref and the chip **leaves the rail** (back to the three seeded rows); and the empty variant paints the instructive empty state ('pin' + 'tt fav'). | §05 R09, §05 R10, §12 R14, §17 R14 | `timer-favorites.png`, `timer-favorites-empty.png` |
| `ACCENT_DISCIPLINE` | The **accent family** — `--accent` (tomato·9, the non-text signal) and `--accent-solid` (tomato·11, the filled-primary background) — appears **only on the sanctioned surfaces**: each view's single `.primary` action (whose **fill is `--accent-solid`**, never raw `--accent` — on-accent-on-tomato·9 is the prohibited D04 pair), the running-state surfaces (card / strip / popover / live-edit), the picker "me" block's **drag grips**, and the active nav item's **icon** (D12 — never the chip or label). The rest of the chrome is monochrome warm grays. The harness captures a whole-chrome computed-style scan over **both** family colours as evidence; the aesthetic judgement is scored over the screenshot. | design.html D04/D11 · §15 | `main-running.png` |
| `ACCENT_SOLID_BUDGET` | **Design.html D11, machine-scored: EXACTLY ONE `--accent-solid`-filled element in every view that has a most-likely action, and never more than one anywhere.** Routing through all five views on the running window and counting every visible element whose computed background is `--accent-solid`: every view counts **≤1** (the ceiling), and every view that marks a **standing primary** counts **exactly 1** (the floor) — Timer its **Stop** while running, Entries its **Add entry**, Clients its **+ Add client**, Reports its **+ New report**; **Settings** marks none and counts **0**, its one primary (the update download) appearing only when an update waits. Both halves gate a real defect: a second accent-solid fill sneaking into a view (a promoted secondary button, an accent-filled Merge) breaks the ceiling, while a view drawing its obvious primary as a `.ghost` — which is how Entries counted **zero at rest** — breaks the floor and rations the colour for nothing. This counts views **at rest**; the states a view reaches when a form opens are `PRIMARY_HANDOFF`. | design.html D11 | `main-running.png` |
| `PRIMARY_HANDOFF` | **The open form owns the accent; the view's standing primary hands it over — exactly one `--accent-solid` fill per view in EVERY state, not just at rest (design.html D11, machine-scored).** `ACCENT_SOLID_BUDGET` counts each view at rest, which is where the budget was already kept. The break lived in the states a view reaches once a form opens: every inline form lit its own commit `.primary` while nothing demoted the view's standing primary, so **three states showed two accent fills at once** — and on the Timer view the two identically-sized, identically-coloured buttons carried the **same word, "Start"** (issue 150). Because the cause is structural (inline forms inherit `.primary`; nothing gave up the standing one), the guard is structural too: it walks each view that marks a standing primary through **rest and through every inline form it can open**, and asserts the visible accent-solid count is **exactly 1** at each stop. The **count** is asserted, never a named selector — pinning "`#toggle` is the lit one" would fight the next restyle (process.html §02) and would not have caught this defect anyway, since both buttons matched their own selector. **Fourteen states**, the audit's own reproduction list extended to every surface the same structure reaches: Timer **idle at rest** / **Details expanded** / **pin-as-favourite field**; Entries **at rest** / **add form open**; Clients **at rest** / **inline rename open** / **add-client field open**; Reports **at rest** / **builder open** (the accent moves off the button that merely opens the builder onto the **Save** that commits it) / **builder nested in its card via Edit** (§12 R08's accordion moves the commit surface into the card's subtree; the handoff must still reach it); the **running** Timer view **at rest** / **with the pin field open** (its standing primary there is **Stop**, the other face of the same standing action — the start panel is idle-only, §12 R05 — so the handoff has to reach both); and the app's one **modal** — the merge-conflict prompt, which mounts on `<body>` outside the views and would otherwise leave the Entries primary lit behind its backdrop. A new inline form that re-lights a second fill, or a deleted handoff rule, flips the pass to false. | design.html D11 | `primary-handoff-timer.png`, `primary-handoff-reports.png` |
| `CLICKABILITY` | **Every clickable text affordance carries the neutral button background** (a non-transparent fill or a visible border); **inert text never does**; the system **accent stays reserved for each view's single primary action** (§15 R-clickability / G10). Over the running main window the harness walks every clickable affordance — `button:not(.primary)`, `.nav-item`, `.nav-link`, `a[href]`, `[data-act]` — and asserts each carries a non-transparent background **or** a visible border (no bare-prose offenders), with the in-control sub-affordances whitelisted (the in-chip `.chip-x`, the `.set-toggle` knob, and anything nested in an already-bordered `.chip`/`.seg`/`.presets`, plus the native multi-select checkbox which draws its own UA affordance); known inert text nodes (`.wordmark`, `.day-head`, `.entry .desc`, `.entry .time`, `.summary`) carry **no** button-like pill fill; and **only** the sanctioned accent-family uses carry `--accent` or `--accent-solid` — `button.primary` (whose **fill is `--accent-solid`**, design.html D11/D04), the running state, and the active nav item's **icon** only (D12 — the chip itself is a lifted paper chip, so a nav-item accent fill is a break) — with at least one primary action carrying the accent-solid fill. **Fails the moment** a clickable affordance (e.g. a `button.ghost` reverting to `background:transparent`) reads as plain prose, or inert text gains a pill background, or a second element steals the accent. | §15 · design.html D11/D12 | `main-clickability.png` |
| `START_ATTRIBUTES` | The Start offers optional inline attribute fields (description, client, project, tags, billable) behind a disclosure — the primary Start stays one-tap — and submitting sends every attribute over the `start` IPC, never a parameterless start. The surface is the GUI **core-entry** surface, **lives in the Timer view** (§12 R5, now `core`), and is **idle-only** (issue #51): the harness routes to the Timer view before opening the disclosure. | §05 R1, §12 R5 | `main-start-attributes.png` |
| `START_FORM` | The **start surface** as a whole (the GUI **core-entry** surface, §12 R5 — now `core` — which **lives in the Timer view**, relocated from the Entries toolbar): the Start offers the inline **description / client / project / tags / billable** form and starts **immediately** over the `start` IPC. The **billable box defaults per the §05 R07 client-keyed rule** (billable when a client is set, else not; always overridable): it opens **unchecked**, **auto-checks when a client is typed** and un-checks when the field is cleared, and an **untouched box is omitted from the payload** so core derives the default — the same rule the one-tap `#toggle` start (a parameterless write) reaches in core, so **both start controls** resolve billable through the one §05 R07 rule. The surface is **idle-only** (issue #51): while a timer runs the whole start panel (`#toggle` + `#start-toggle` + `#start-form`) is **hidden**, so the running Timer view offers **only edit-or-stop** — and there is **no `#switch` element** in either run state (Switch removed, issue #34; core's `start` remains the atomic stop-then-start for `tt` and programmatic callers, §05 R01). Machine-scored facts: the opened idle form exposes all five attribute controls and the idle primary button reads `Start`; the billable box opens unchecked, checks on a typed client, un-checks on a cleared one, and an untouched submit's captured `start` payload carries **no `billable` key**; the running snapshot **hides the start panel** (no visible `#start-form` / `#start-toggle`) while Stop + the live-edit strip stay visible, with no `#switch` element. | §12 R5, §05 R07 | `main-start-form.png`, `main-start-form-running.png` |
| `RUNNING_SINGLE_ACTION` | **While a timer runs, the Timer view offers ONLY edit-or-stop of the running entry (§12 R05, issue #51).** The whole start panel is hidden — no visible `#start-form`, `#start-toggle`, or one-tap `#toggle` — so **exactly one Description field** paints (the live-edit strip's `#le-desc`; the Details form's second Description field is gone with its panel), and the only start-shaped affordances left are the primary **Stop** and the live-edit strip. There is no "start another" affordance until the running entry is stopped; core's `start` stays the atomic stop-then-start for `tt` and programmatic callers — only the GUI surfacing of a start control while running is removed. Machine-scored facts: on the running snapshot the Timer view paints exactly one visible Description field and it is `#le-desc`; `#start-panel` is hidden with no visible `#start-form`/`#start-toggle`/`#toggle`; `#timer-stop` and `#live-edit` are visible; and no `#switch`/`#timer-switch` element exists. Would fail the moment a second Description field or any start affordance reappears while running. | §12 R05, §12 R04, §12 R14 | `timer-running-single-action.png` |
| `UNIFIED_FORM_ADD` | The manual-add surface is the **one unified entry form in ADD mode**, inline in the Entries view (**no modal**) — the same form edit mode uses (§12 R06). It is a **two-column card** (`.unified-form[data-mode=add]`): **LEFT** — a 3-line multiline description `<textarea>` (`#add-desc`, §05 R10), client + project **`<select>`s** (`#add-client`/`#add-project`, populated from the same source `tt` uses), a **tag chip host** (`#add-tag-chips`), and the **billable** toggle; **RIGHT** — the **inline interval picker** (§12 R15, `#add-picker`: a **month calendar** + a **single-day hour-line column** with the draggable accent **"me" rectangle**) over the **collapsed Start/Stop expander** (§12 R17, `#add-times-toggle` `aria-expanded=false` over the hidden raw text fields `#add-from`/`#add-to`). The form carries **NO `type=datetime-local`** anywhere (G1). Other picker entries render **gray** (`.stp-block.other`); an **overlapping span paints yellow** (`.stp-overlap`, **warn-only**, `pointer-events:none`); **accent discipline (design.html D11 / V3):** the **"me" rectangle is the outline idiom** — computed `--accent-weak` fill + a ≥1px `--accent` border + **ink** time labels (solid accent is reserved for primary actions; a solid-accent me-fill fails) — and **Save entry** is the view's single **`--accent-solid`-filled** primary. **Dragging the "me" block updates the form's start/stop state LIVE** — the raw `#add-from`/`#add-to` fields change (span preserved on a body drag) before any Save (G7). **Save entry is the SOLE commit:** `window.__ADDED__` carries the **picked (post-drag) `fromLocal`/`toLocal`** plus `description`/`client`/`project`/`tags`/`billable` over the single `add` IPC (parity with `tt add`); the overlapping backfill still saves (the form closes) and the non-blocking inline overlap banner (`#overlap-banner`, `role=status`+`aria-live=polite`) raises (§06 R4). Machine-scored facts: two-column unified form with the left fields + right inline picker (month cal + day column) + collapsed expander, no datetime-local; ≥1 gray other + ≥1 yellow (inert) overlap; me = accent-weak fill + accent border + ink labels, Save = accent-solid fill; body drag changes both `#add-from`/`#add-to` (span preserved) live; Save sends the picked span + attributes over the single `add`; form closes + overlap banner shows. Fails if the form is not the unified two-column form, still carries a datetime-local, the picker does not drive form state live, the me block regresses to a solid accent fill, or Save is not the sole commit. | §12 R07, §12 R15, §12 R17, §05 R10, §06 R4 · design.html D11/V3 | `unified-add.png` |
| `UNIFIED_FORM_EXPANDER` | The unified add form's collapsed **Start/Stop expander** is the exact-entry escape hatch and the **only path for an OVERNIGHT span** (§12 R17). **Collapsed by default:** the raw Start/Stop text fields are hidden (`#add-times-body[hidden]`, `#add-times-toggle` `aria-expanded=false`) while a **tabular echo** of the current interval shows beneath the calendar (`#add-picker .stp-echo.tnum`, e.g. `22:00 – 23:00`). **Clicking the toggle reveals the two raw `type=text` fields** (`#add-from`/`#add-to`, **not** native `datetime-local`, G1). **Typing an overnight span** (start `2026-06-24T22:00`, stop `2026-06-25T02:00`) into those fields feeds the **ONE shared interval state** the drag also mutates — the picker column reflects the typed **start** (the `.stp-block.me` block's `top` maps to 22:00 = minute 1320 on the 720px/24h track) and the collapsed **echo reflects the cross-midnight span** (`22:00 – 02:00`), while the raw stop field keeps the **next-day value verbatim** (authoritative, never flattened to the start's day). **Save entry is the SOLE commit:** `window.__ADDED__` carries those **exact** overnight `fromLocal=2026-06-24T22:00`/`toLocal=2026-06-25T02:00` over the single `add` IPC (parity with `tt add --from --to`). Machine-scored facts: collapsed echo present + tabular with fields hidden; the toggle reveals two raw text fields; the typed overnight updates the shared interval so the echo reads `22:00 – 02:00` and the `me` block sits at minute 1320 while `#add-to` keeps `2026-06-25T02:00`; Save sends the exact typed overnight span. Fails if the expander fields don't feed the shared interval (the picker/echo stay stale), Save reads a stale picker-only value, or the overnight stop is dropped / coerced to same-day. | §12 R17, §12 R15, §05 R05, §06 R4, G1/G2 | `unified-form-expander.png` |
| `UNIFIED_FORM` | Editing an entry opens the **one unified entry form in EDIT MODE in the SAME view-level host add mode uses** (`#entry-form-host`) — **NOT crammed into the ~124px calendar day column** — in the view flow (**no modal / backdrop / dialog** chrome; the form host is in normal flow, `position:static`). The scene asserts **host identity explicitly**: opening add mode records its host, and the edit form mounts in **that very same element**; the edited calendar event keeps its content and carries the subtle **`.editing`** selection state. **Both a click on the entry and its Edit affordance open it.** It is **seeded from the entry**: the `.edit-desc` control is a multiline `<textarea>` carrying the description, the client + project selects are **pre-selected** to the entry's client/project (carrying entity **ids**), the tag chips list the entry's tags, the billable checkbox matches, and the collapsed **Start/Stop expander** holds the seeded Start + Stop. **§12 R15 — the inline interval picker (successor to the retired picker-modal scene):** the form mounts the interval picker **IN FLOW** in its own `.edit-picker` host (**no `.stp-backdrop`, no `.stp-apply` anywhere**; the `.stp-inline` host computes `position:static`) — a **month calendar** + a **single-day `.stp-track`** with hour lines. A known **BODY drag** advances **BOTH** bound form fields (`.edit-start`/`.edit-end`) by the snapped **5-min** amount (span preserved); a bottom **`.stp-resize`** drag advances **only** `.edit-end`; ≥1 gray **`.stp-block.other`** paints and an **inert yellow `.stp-overlap`** (`pointer-events:none`, warn-only, never blocks) paints where the edited span overlaps a neighbour. Every write is **LIVE** into the form's own fields — **no commit fires until Save entry** (G7). *(The running-entry **start-only** variant — `.stp-block.me.open` mask + a start grip only, no `.stp-resize`/end label/echo, no end value — is asserted by `TIMER_VIEW`.)* **Exact stored times (issue #49):** opening the deliberately non-5-min-aligned entry 84 (09:07:33→11:03:00Z) renders its stored start/stop **exactly, to the second** (`.edit-start` carries the `:33` seconds; `.edit-end` sits off the 5-min grid) — never snapped on open; **Save entry with NO drag** sends a patch carrying **no `startUtc`/`endUtc` at all**, so the store's times round-trip unchanged; and a bottom-grip drag lands the stop **on the :05 grid** while the untouched start keeps its seconds — the snap applies only to the actively dragged handle. **Save entry is the sole commit** and sends a patch of **only the changed fields** over the `edit` IPC (`tt edit` parity). The **edit-mode footer** carries a **Split** control (`window.stint.split`, closed entries only) and a **two-step Delete gate** that **ARMS** (a worded `confirm-delete` affordance appears, nothing removed) then **CONFIRMS** (`window.stint.remove` fires exactly once with the entry id). **Accent discipline (design.html D11):** only **Save entry** carries the accent-solid fill in the footer — it is the single `.primary` there. **§12 R10 — flags in context (the editor is where the flag DETAIL lives now the list is gone):** opening the editor on an **overlapped** entry shows the **overlap detail** (`.ef-flags .banner.overlap`) spelling out the amount + which neighbour ("Overlap: 30m with previous/next entry"); opening it on a **slept-through** entry shows the **reversible sleep control** (`.ef-subtract`, label toggling **Subtract slept ↔ Restore** off the core-fed `excludedSeconds`) — after subtract the **raw duration reads struck through** (`.ef-dur s.struck`, line-through, 04:00:00) beside the **trimmed billable** (03:00:00), and subtracting again **restores** it (the struck raw disappears). Machine-scored facts: add mode and edit mode mount in the SAME `#entry-form-host` element; click + Edit both open the form there (the event carrying `.editing`); no backdrop/dialog and static host; description textarea + client(`1`)/project(`11`) selects + tag chip(`deep`) + billable + Start/Stop all seeded; the inline picker mounts in flow (no `.stp-backdrop`/`.stp-apply`, static host) with a month calendar + `.stp-track` + hour lines, a body drag moves both `.edit-start`/`.edit-end` by the snapped 5-min amount (span preserved) while a `.stp-resize` drag moves only the stop, and ≥1 gray `.stp-block.other` + ≥1 inert yellow `.stp-overlap` (`pointer-events:none`) paint; footer has Split + the two-step Delete (arm then confirm removes `{ id: 80 }`); Save patches only the changed description; the overlapped entry's editor shows overlap detail matching `/Overlap:\s*\d+m\s+with\s+(previous|next)/`; the slept entry's editor shows the reversible Subtract/Restore control with the struck raw (04:00:00) beside the trimmed billable (03:00:00), reversible on a second click. the exact-times entry (84) opens showing 09:07:33 (seconds preserved, stop off the 5-min grid), a no-drag Save patches neither `startUtc` nor `endUtc`, and a stop-grip drag snaps only the stop to :05. Fails if edit mode is a modal, mounts anywhere but the shared `#entry-form-host`, omits a seeded field, a modal/backdrop or Apply returns on the picker, a drag stops writing the form fields live or off-snap, an overlap blocks, the footer lacks Split or the two-step Delete gate, or the overlap detail / reversible subtract / struck raw duration is missing, or opening an entry snaps/rewrites its stored times (an un-dragged Save that changes start/stop is silent data corruption). | §12 R06, §12 R15, §06 R1–R2/R4, §05 R6, §12 R10 · design.html D11 | `main-edit.png` |
| `MULTILINE_DESC` | The entry form's **description control is a 3-line scrollable multiline `<textarea>`** (`rows=3`, `overflow-y:auto`, vertically resizable), and a stored description carrying an **embedded newline renders VERBATIM** in the form — its interior line break is preserved, **never flattened to a single line** (§05 R10 / §12 R07). Descriptions are stored verbatim (no surface flattens the text); the CLI table cap (first line, 60 chars + `…`) and the CSV round-trip are transcript/GOLD evidence, and this form field is the GUI half. Machine-scored facts: opening the multiline entry's edit form, the `.edit-desc` control's `tagName` is `TEXTAREA` with `rows=3`, its computed `overflow-y` is `auto`/`scroll`, and its `.value` is exactly `line one\nline two` (contains the interior `\n`). Fails the moment the control is a single-line `<input>`, drops the newline, or is not scrollable. | §05 R10, §12 R07 | `main-multiline-desc.png` |
| `DELETE_CONFIRM` | Delete is destructive, so it requires a confirm step: the first click swaps the button into an explicit "Confirm delete?" affordance with a Cancel — the entry is not removed until the explicit confirm tap. | §06 R1 | `main-edit.png` |
| `CONFIRM_DESTRUCTIVE` | **Destructive actions confirm before acting (§17 R11).** The §17 framing of the gate: a **single Delete click must not destroy the entry** — it surfaces the in-window confirm (`.confirm` with `confirm-delete` + `cancel-delete`) and the entry is **still present** after the arming click (`window.stint.remove` not called, `__REMOVE_CALLS__` empty). **Only the explicit confirm** removes it, after which the entry is **gone from the list** (the remove mock drops it from the snapshot, so the post-confirm reload reflects the real deletion). Machine-scored facts: entry present pre-confirm with zero remove calls, then absent post-confirm with exactly one `remove({ id: 20 })`. (Archive-when-referenced reuses the same gate — see `CONFIRM_ARCHIVE`.) | §17 R11, §12 R13 | `main-confirm.png` |
| `CONFIRM_DELETE` | **Destructive actions confirm in the window (§12 R13 — `core`, loss-protection: destructive-action confirmation per §03).** A **single stray Delete click must not destroy an entry**: clicking Delete surfaces the in-window confirm gate (the generic `.confirm` affordance with a `confirm-delete` + a `cancel-delete` control) and the instrumented `window.stint.remove` is **not** called by that first click (`__REMOVE_CALLS__` stays empty). **Only the explicit confirm** removes the entry, and it fires `remove` **exactly once**, carrying the entry id. (The same gate is the GUI counterpart to `tt rm`'s confirm / `--force`; archive-when-referenced reuses it — see `CONFIRM_ARCHIVE`.) Machine-scored facts: the confirm gate appears, `remove` calls after the arming click = 0, and exactly one `remove({ id })` fires on confirm. | §12 R13 | `main-confirm-delete.png` |
| `SPLIT_AFFORDANCE` | A **closed** entry exposes a discoverable Split control that opens an inline instant picker (defaulting to the span's midpoint) and calls the split capability over the `split` IPC with a UTC instant inside the span; the **open/running** entry exposes no Split control (only a bounded span can be cut). The renderer adds no arithmetic — core enforces the strictly-in-span rule. | §06 R2, §12 | `main-split.png` |
| `INLINE_GATE_CONTAINMENT` | **A transient gate armed from a calendar event is a LAYER OVER the calendar, not content of the 124px day column whose button armed it** (design.html D09, issue #146). Both gates the calendar arms — the split picker (`.split-at`) and the two-step delete confirm (`.confirm`) — shipped laid out **in flow inside that column** and **with no surface of their own**: the split picker measured **348px against a 124px column**, running 264px past the column and, armed in the **leftmost** one, **16px off the left edge of the window**, over a **transparent** background with a **0px radius and no shadow** — so a raised region read as loose text floating over the entries, and half its controls (the "Split at" label, the instant field) were clipped away entirely by the calendar's own scroller. Driven through the **real hover affordance** on the two **EDGE** columns (`edgeColumnState` pins entry 40 to the week's first day column and entry 41 to its last), never read off the stylesheet, so the check survives a restyle: whatever the gates are made of, each must **(a)** sit **wholly inside the calendar as the user can see it** — the `.cstrip` scroller's visible box (border box less scrollbar gutters) intersected with the window — **(b)** resolve to a real surface **on the elevation ladder**: an **opaque** background that is the `--paper` token, a **non-zero radius**, and a **shadow** — and **(c)** be **VISIBLE where it lands**, which landing inside the scrollport no longer implies: issue #145 pinned two opaque sticky axes inside that same box (the day-header band `.dh` at z-8, the hour gutter `.gut` at z-9) that outrank the ops chip the gate is mounted in, and an overlapping neighbour's own chrome (its corner checkbox is z-6) competes there too. So each gate must **clear both bands' rects** and **hit-test as its own element across a grid sampled over its face** (inset past its corner radius — a rounded corner is not part of the element, so probing the literal corners would read "occluded" on every pass), with **every control inside it hit-testing to itself** at its centre. Entry 42 **overlaps** entry 40 for that reason: without a neighbour the layer would only ever be probed over empty track, and nothing would hold its rank against the chrome it actually covers — with the host block left at the ops chip's own rank, the neighbour's chip punches through the gate's face. Both edges are probed because a clamp that only pulls the left edge in still spills off the right, and the gates grow leftward from the ops chip so the two columns fail in opposite directions. The gate's width against its column is **reported, not gated** — a future redesign may legitimately shrink it; what must not return is a gate that escapes the calendar, carries no surface, or hides under something. Machine-scored facts: the split picker armed on the first column and the delete confirm armed on the last each report `inside=true` with zero escape left or right, `clearsStickyAxes` + `topmost` + `controlsReachable` all true, and `opaque` + `paper` + `raised` + `rounded` all true. Fails if either gate crosses the calendar's visible edge, overlaps a pinned axis, is painted over by any chrome, or paints no fill, radius or shadow. | design.html D09, §06 R1–R2, §12 R13, §12 R16 | `main-inline-gate.png` |
| `OVERLAP_BANNER` | A write that creates an overlap surfaces a **non-blocking inline banner at the moment of the edit** — not only the durable per-row flag — worded as allowed-but-flagged ("This entry overlaps … — allowed, but flagged in reports."). The banner is `role=status` + `aria-live=polite` (announced, accessibility-tree visible) and auto-clears on the next refresh that carries no warning; the write still commits (overlap is warned, not blocked). It uses the `--flag` tokens, never the accent. | §06 R4, §12 | `main-overlap-banner.png` |
| `WRITE_REJECTION_FEEDBACK` | A **refused core write is surfaced where it was attempted, never silently swallowed** (§12 R21). Driving the real renderer over a **strict-rejecting mock** (`rejectWrites` — edit/split/rename/toggle each reject with a StoreError-shaped message, the strict-`listEntries` precedent), each site **catches and displays**: the **form stays OPEN** and an **announced** (`role=status` + `aria-live`) inline `.form-error` region carries the reason. Four folded facts — (a) unified-editor **Save**: the `edit` IPC rejects, the editor stays open with the message in `.ef-warning`, and `window.__EDITED__` stays unset (the refused write recorded nothing); (b) **split confirm**: the `split` IPC rejects, the picker stays open with `.split-warning`; (c) **inline rename** (client): `renameClient` rejects, the rename form stays open with `.rename-warning`; (d) **Stop/toggle**: `toggle` rejects and the rejection is **VISIBLE on the Timer view** where the Stop was clicked — the announced `#timer-warning` region beside the card controls (the "Stop appears dead" surface, issue #61), with the Entries `#overlap-banner.error` mirroring the block. **What the user reads (issue 138):** every mock rejects in **Electron's real `ipcRenderer.invoke` shape** — its wrapper sentence plus the thrown class name around the reason, the string the packaged app produces — so each of the four regions is scored on its rendered text: the reason **exactly**, with no "Error invoking remote method", no "StoreError", and no `--flag` name (the app shipped all three at once). The error component uses the `--danger` block palette, distinct from the `--flag` overlap advisory. Fails if any site swallows the rejection (form closes / no message / not announced) or the write records despite the refusal. | §12 R21, §05 R11, §06 R2, §13 | `main-edit-reject.png` |
| `ADD_REFUSAL_PALETTE` | **On the add form a refused write reads as a BLOCK and an allowed-but-flagged one reads as an ADVISORY — two palettes, one region, never inverted** (design.html D15, issue 139). The form's `#add-warning` carries BOTH message kinds, so colour (not location) is what tells them apart; the app shipped the inversion — a refused Save wore the `--flag` amber advisory chrome, so the colour said "saved with a caveat" while the sentence said "nothing was written". Driving the real renderer, three folded facts, each scored on **computed** colour (a palette is exactly what a screenshot-only check lets rot), with paint transitions suppressed so every probe reads the cascade and not a frozen mid-fade frame: (a) **refused Save** over the strict-rejecting mock (`rejectWrites` — the `add` IPC rejects like core refusing an inverted span, in Electron's real wrapped shape) leaves the form **open** with the **announced** (`role=status` + `aria-live`) `#add-warning` carrying the reason **alone** ('stop time must be after start time' — no invoke wrapper, no `StoreError`, no `--flag` name, issue 138), records **nothing** (`window.__ADDED__` stays null), and paints text/background/border as `--danger` / `--danger-weak` / `--danger`; (b) **the region's other state** — Cancel then reopen returns it to its `--flag` / `--flag-bg` / `--flag-line` base chrome with the `error` state class gone, so a refusal cannot outlive its message and dress the next advisory as a hard block; (c) **the advisory sibling** — the same form saving an **overlapping** backfill COMMITS (`__ADDED__` set) and raises its 'allowed, but flagged' banner in the `--flag` triple. Plus the split itself: the two token triples must **differ**, so a token edit collapsing danger onto flag cannot make (a)–(c) pass vacuously. Fails if a refusal reads flag, an advisory reads danger, the two states resolve alike, or the refused backfill records. | design.html D15, §12 R21, §06 R4 | `add-refusal-palette.png` |
| `MERGE_CONFLICT` | Selecting **two-plus contiguous closed entries** — via the **hover-corner checkboxes** on the entries calendar's events (§12 R16), which arm a **multi-select mode** — reveals the **merge selection bar ABOVE the calendar** (design.html D11 / V5, mock `main.html` `.selbar`): its raised-chip **count pill reads "N selected"** (`#merge-count`, here `2 selected`), and its **Merge action is a NEUTRAL small button** (`#merge-go`, static label **"Merge"**, **never `.primary`** — the Entries view's single accent-solid primary is the add form's Save entry). The bar is hidden with fewer than two selected. Merging entries that **disagree** on client/project or billable raises the conflict prompt offering the distinct client/project choices **and** a billable choice **before** any merge commits. The user picks which value the merged entry keeps (descriptions are concatenated and tags unioned by core regardless). An **agreeing** selection merges directly, with no unnecessary prompt. The prompt is **hosted in `app.js`** (the `.editor.conflict-prompt` modal), styled to `merge-conflict.html`. The renderer resolves no names: it sends the winning entry's id (`winnerId`) + the chosen billable over the `merge` IPC, and the main process maps that to core's `MergeOptions`. **Escape dismisses the prompt as a cancel** (issue 147, craft checklist §4 — "Esc closes/cancels the innermost thing"): pressing Escape while the prompt is up unmounts it *and* commits nothing — same outcome as its own Cancel, no field resolution applied. Machine-scored sub-facts: bar hidden at 0/1 selected; at 2 selected the bar shows above the calendar host with the `2 selected` pill and a Merge button carrying no `primary` class; after Escape neither `.editor.conflict-prompt` nor `.editor-backdrop` is in the DOM and no `merge` payload was sent (the outcome, not just the dismissal — an Escape that silently confirmed would also empty the DOM). | §06 R3, §12 R6, §12 R16 · design.html D11/V5 · craft checklist §4 | `main-merge-conflict.png` |
| `MERGE_CHOICE_LIFT` | **The merge-conflict modal's chosen option is the raised paper chip, never an accent fill (design.html D12).** The app's only modal (STATES.md) was where D12 was most plainly abandoned: the chosen `.mc-opt` filled `--accent-weak` behind an accent border and an inset accent hairline with an accent radio dot, against an unchosen sibling at plain paper — a selection that turned accent, in the one dialog whose whole job is "which of these do you choose". A CSS comment conceded the deviation; the issue-#144 triage did not preserve it, because a documented exception suppresses its own rediscovery. The repaired idiom is the segmented one (`.seg-btn.on`) at dialog scale: the dialog body is paper, so an unchosen option must SINK for the chosen one to read as raised. Driven over `mergeConflictState` (two closed contiguous entries disagreeing on client AND billable, so the prompt offers two real choice groups). Machine-scored facts: **(a) the chosen lifts** — every `.mc-opt.on` computes the `--paper` fill plus a non-none box-shadow; **(b) the unchosen sinks** — every peer is flat (box-shadow none) and NOT paper, so the lift is a measured contrast and not a shadow nobody can see; **(c) no accent in the prompt** — no option, chosen or not, paints an accent-family colour as fill, gradient, border or shadow, and the whole modal holds ZERO `--accent-weak` fills; **(d) colour is not the sole cue (A05/D05)** — the chosen rows' `.rad::after` dots render and the unchosen rows' do not, so the filled-vs-empty shape carries the state on its own; **(e) the chip follows the choice** — clicking the other client option moves `.on`, the paper fill and the lift onto the newly chosen row and off the old one, with no accent appearing on either. | design.html D12 · §06 R3, §12 R6 | `merge-choice-lift.png` |
| `MERGE_NOCONFLICT` | The no-unnecessary-question counterpart to `MERGE_CONFLICT`: a corner-checkbox multi-selection of **two CONTIGUOUS closed entries that AGREE** on client/project and billable, then **Merge**, fires the merge **directly — with no *conflict* prompt** (there is nothing to resolve) **and no gap confirm** (the selection is contiguous, so nothing is fabricated), §06 R3. This asserts the absence of an *unnecessary* prompt, **not** that the agree path never gates — a non-contiguous agreeing selection still gates (`MERGE_GAP`). The renderer sends just the selected **ids** over the `merge` IPC (no `winnerId`, no `allowGap`); core does the actual fold (descriptions concatenated, tags unioned). Machine-scored facts: no `.editor.conflict-prompt` and no `.confirm-gap` appears, `window.stint.merge` is called exactly once carrying the two ids, and the payload carries **no** `winnerId` and **no** `allowGap`. | §06 R3, §12 R6 | `main-merge-conflict.png` |
| `MERGE_GAP` | The contiguity gate (§06 R3, §12 R13). A corner-checkbox multi-selection of **two closed entries that AGREE on client/project and billable but are NOT contiguous** — a positive gap sits between them (09:00–10:00 then 14:00–15:00) — then **Merge**, must **not fold silently**: the Merge button first swaps into an in-window **confirm stating the resulting span/duration** (09:00 → 15:00, `06:00:00`) and the fabricated **`04:00:00` gap** it would bill, carrying `data-act="confirm-gap"` / `cancel-gap` hooks (the §12 R13 `confirmInline` delete-confirm precedent). **No merge commits on the arming click.** Only the explicit **Merge anyway** tap fires `window.stint.merge`, and the payload then carries **`allowGap: true`** so core accepts the fold. This is the regression guard for the filed bug — two 1-hour entries days apart folding into one 78-hour billable entry on a single click with no confirmation. Machine-scored facts: clicking Merge on the gapped selection shows a `.confirm-gap` naming the non-contiguity + the `06:00:00` span + the `04:00:00` gap, with confirm/cancel controls and **no** `merge` payload sent yet; the explicit confirm then sends exactly one `merge` carrying the two ids and `allowGap: true`. | §06 R3, §12 R13, §12 R16 | `main-merge-gap.png` |
| `CLIENTS_VIEW` | The **Clients** nav view lists the active clients with their projects nested under their client, and offers create/rename/archive **in place** (an Add client control, a per-client Add project / Rename / Archive, and per-project Rename / Archive). Archived items drop out of the active list (archive hides from the active list but keeps history — the entry labels stay resolved, not copied). The create affordances are **driven, not merely present** (issue #48 — a duplicate `id="add-client"` dead-ended the button while every presence-only check passed): clicking **+ Add client** opens the inline "New client" field, a typed name commits over the `addClient` IPC, and the new client **lands in the active list**; the same driven fact holds for **+ Add project** on a client row (the payload carrying that client's id — the renderer resolves no names) and **+ Add tag** on the tag strip. **Empty reference data (STATES.md Clients × empty, the `emptyRefData` fixture knob):** the never-populated view **instructs instead of blanking** — the "**No clients yet**" copy mentions `tt client add` and the "**No tags yet**" copy mentions `tt tag add`, with zero client/tag rows. **Icon-only affordances are named (design.html D16, machine-scored):** every visible `.iconbtn` in the view carries a non-empty accessible name (aria-label / title / text). **Focus order follows the visual order (design.html A04, machine-scored — issue 161):** a Tab-walk of the view with **"show archived" ON** — the view's one ordering hazard, since archived clients and archived tags are appended in a **second pass** after every active row — advances in **reading order** at every step: down the page, and left-to-right between controls sharing a row. Each stop is measured **relative to the `#clients` section's own box**, not to the viewport, and that is the whole point of the row: the design audit reported this view's tag rows "visited bottom-first" (`976,763 → 938,623`), but the DOM order was never wrong — **the measurement was**. Tabbing to a control below the fold scrolls it into view, so the next stop's *viewport* y is smaller even though it sits lower on the page (reproduced: the same two adjacent, correctly ordered rows read `398 → 210` viewport-relative and `398 → 444` page-relative). A page-relative reading cannot be faked by a scroll, in either direction. Guard-the-guard: the walk must find **≥15** stops, so a walk that has gone blind fails instead of passing on an empty set. The view chrome — its create "+" icons included — is monochrome system grays: icons take accent only when their item is active (design.html D16), and the accent stays on the primary action / running state only (§15 accent discipline). | §07, §12 · design.html A04 | `main-clients.png`, `main-clients-created.png`, `main-clients-empty.png` |
| `CONFIRM_ARCHIVE` | **Archiving a REFERENCED client/project confirms in the window (§12 R13).** Archiving a client/project that carries history (the core-fed `referenced` flag) is destructive, so it reuses the same two-step gate as Delete. Driving the real renderer: clicking the referenced Acme client's **Archive** surfaces the in-window `.confirm-archive` gate (a `confirm-archive` + `cancel-archive` control), Acme **stays listed**, and the instrumented `archiveClient` is **not** called by that first click (`__ARCHIVE_CLIENT_CALLS__` empty — a stray click archives nothing). **Only the explicit confirm** archives it — exactly once, carrying Acme's id — after which Acme detaches. An **unreferenced** client archives directly (no confirm — R13's exact scope; that path is the Globex archive `CLIENTS_VIEW` drives). Machine-scored facts: the confirm gate appears with Acme still listed and zero archive calls after arming, then exactly one `archiveClient({ id: 1 })` on confirm. | §12 R13 | `main-confirm-archive.png` |
| `RESTORE_ARCHIVED` | **Archive is a reversible hide (§12 R13).** Archived records are out of the active list by default; a **Show archived** toggle reveals them (each with an "archived" pill) carrying a **Restore** button, and Restore returns the record to the active list. Driving the real renderer: the archived client/tag are **hidden by default**, the toggle reveals them with their Restore buttons (client card + tag row, each with a `.pill` and `data-act="restore-client"`/`restore-tag`), and clicking the archived client's **Restore** fires `restoreClient({ id })` and lands it back **active** (no longer `.archived`). Machine-scored facts: archived client/tag absent before the toggle, present with Restore after it, and the restored client back in the active list carrying the restore payload. | §07, §12 R13 | `main-clients-archived.png` |
| `TAG_CHIPS` | An entry's tags appear **as chips on its own calendar event** — not in a separate list — and the running entry's tags also show on the summary line. The chips are **monochrome** system grays (the accent stays on the primary action / running state only, §15). There is **NO per-row Edit-tags control** (DELETED, #43 — no `[data-act="tags"]` survives on any event); tags are edited in the **unified form's tag-chips editor** (§12 R06 / G6, `.ef-tag-chips` removable chips + an `.ef-tag-add` "add a tag…" input). The scene asserts the capability end to end: open the closed entry's unified form, **remove** the seeded `meeting` chip and **add** a `billing` chip in the form's editor, Save, and the `edit` patch carries the minimal `addTags:['billing']`/`removeTags:['meeting']` and **touches only tags** (parity with `tt edit`). Deterministic: the fixture's open event carries 2 tags and its closed event 1, so exactly 3 chips paint across the events. Fails if a per-row tags control survives or the form's chip editor cannot add + remove a tag over the one `edit` commit. | §07, §12 | `main-tags.png` |
| `REPORTS_VIEW` | The **Reports** view is the in-shell, PRIMARY surface for **saved, named report definitions** (G11) — it replaces the retired standalone `report.html`, so the **sidebar is present** with **Reports active**. Routing to it paints a **list of saved-definition cards**, one per saved def, each showing its **name + a spec summary** (the stored range + group-by + filters + rounding) and **Run / Edit** affordances (+ a kebab → Rename / Delete). The single accent affordance in the whole view is the **+ New report** primary action, **filled `--accent-solid`** (design.html D11 / V6 — a raw `--accent` fill under its on-accent label would be the prohibited D04 pair; the cards, the builder, and the run-output stay monochrome, painting neither accent-family colour). The view is an **accordion** (issue #268): what a card owns expands **inside that card**, **exactly one card expanded at a time**, and **collapse discards** — collapsing a card clears its results, so re-viewing a report costs a re-run. **+ New report** (and a card's **Edit**, which opens populated via `showReport`) reveals the **inline builder** — nested **inside the edited card's subtree** for Edit (that card the one expanded card), at **view level below the list** for New (a fresh def has no card yet) — a name input, a **range** segment (the five presets + **Custom…**, whose custom range is a **pair of plain date fields**: `#rep-range-from`/`#rep-range-to` are `input[type="date"]`, **no time component**, zero `datetime-local` inputs in the builder — §09 R01 / G3), a **group-by** segment (Client / Project / Day / Tag), **client / project / tag** filters, a **billable** segment, and a **rounding** toggle + 6/10/15/30 increment picker — saving over `saveReport` / `editReport` (parity with `tt report save` / `tt report edit`). Saving a filled Custom def sends a `rangeSpec` that is **exactly** `{ kind:'absolute', fromDate, toDate }` — the two raw `YYYY-MM-DD` strings, never a derived UTC instant, no `'T'` — and the saved card's spec summary prints the date pair verbatim (`Custom: <from> – <to>`). Clicking **Run** on a card resolves+runs the definition through core (`runReport`, parity with `tt report run`) and **expands that card** around the **grouped run-output** — the results visually scoped to the report that produced them — with the **resolved-range header** and **overlap + unreviewed-sleep flags in context** on the affected rows (the overlap chip on Globex / Q3 Strategy, the unreviewed-sleep chip on Initech / Market research — reusing the `REPORT_SUMMARY` shape), **not** in a separate list, using the `--flag` tokens, never the accent. Running a **second** report collapses the first card (its body genuinely emptied — the results move, they are not copied) and expands the second; the run-output's **close control** collapses the accordion entirely and **discards** the results. The view offers **TWO distinct exports** (issue #72), differently armed: the report's own **Export CSV / Export JSON** belong to the run-output **inside the expanded card** — **computed-invisible before any run** (outcome, not attribute — issue #262's pre-run dead buttons), carrying **no scope label** (the nesting states the scope) — and drive `exportEntries` with **`scope: 'filtered'`** + the saved ref (the rows the report shows — byte-identical to `tt report run <name> --csv|--json`); **Export All Data (CSV/JSON)**, set apart at the **bottom of the view — view-level, inside no card** (run-independent, so it stays outside the accordion), is **standing chrome — always visible and working, no run required, no disarmed state** — and drives **`scope: 'all'` with NO saved ref** (the **whole record**, every raw entry ever — byte-identical to no-flag `tt export`); the raw status's honest **"(all data)"** wording keeps it from being mistaken for the report's rows. The renderer resolves no names (it sends client/project **ids**) and re-derives no range/grouping/rounding/total. Machine-scored facts: one card per saved def with name + spec summary + Run/Edit; the sidebar present with Reports active; + New report the only accent-family paint in the view (its computed fill `--accent-solid`); + New / Edit open the builder with every control (presets incl Custom, the four group-bys, filters, billable, rounding+increment); the two custom-range inputs are `type="date"` (no `datetime-local` in the builder) and stay tucked behind **Custom…** until it is chosen; filling `2026-06-01`/`2026-06-07` and saving fires a `saveReport` whose captured `rangeSpec` is exactly `{ kind:'absolute', fromDate:'2026-06-01', toDate:'2026-06-07' }` with the new card's summary printing the pair; the New builder sits nested under **no** `.def`; Edit re-opens the matching def populated with a Delete affordance, its builder **nested inside that card** — the one `.open` card; Run sends the card's name and paints the grouped totals with both flags on their affected rows (none outside the table) + the resolved-range header, the run-output **and** the filtered export row inside the ran card's subtree, exactly one `.open` card, no `.report-export-scope` in the filtered row, `#rep-run-export-all` inside no card; **one-run-at-a-time computed state** — running the second def moves `#rep-run` into ITS card (the only `.open` card) and leaves the first card's `.def-body-inner` empty; clicking `#rep-run-close` leaves zero `.open` cards, `#rep-run` + `#rep-run-export` computed-invisible with the painted rows GONE (discard on collapse), Export All Data still standing; **pre-run computed state** — `#rep-run` + `#rep-run-export` computed-invisible, `#rep-run-export-all` computed-visible, a pre-run **Export All Data (CSV)** click firing a real `exportEntries` call with `scope: 'all'`, **no `savedReportRef`**, status matching "(all data)"; after a run the filtered **Export CSV/JSON** invoke `exportEntries` with `format: 'csv'`/`'json'`, `scope: 'filtered'`, and the saved ref; the bottom **Export All Data (CSV)/(JSON)** still send `scope: 'all'` with **no saved ref** and are labelled "Export All Data". **§12 R21 Save-refusal sub-facts:** an **incomplete custom range** (name it, pick Custom…, fill only From, Save) is a **fed-back no-op** — **zero** `saveReport` calls, the **builder stays open**, the **missing field (`#rep-range-to`) takes focus**, and `#rep-warning` carries a persistent message; a **duplicate name** (save under an existing def's name) is **refused by core** and its `#rep-warning` message **PERSISTS past the tick** (no same-tick self-erase), the builder stays open, and no third card appears; an **inverted absolute range** (From after To) is refused by core and `#rep-warning` reads the reason ALONE — the mock rejects in Electron's real `ipcRenderer.invoke` shape, so a painted invoke wrapper or exception class fails the item (issue 138). **Zero saved defs (STATES.md Reports × empty):** with no saved definitions the visible `#rep-defs-empty` state reads exactly "**No saved reports yet.**" with zero cards — never a blank list. | §12 R08, §09 R08, §09 R09, §12 R21, §18 · design.html D11/V6 | `reports-list.png`, `reports-run.png`, `reports-empty.png` |
| `ENTRIES_CALENDAR` | The Entries view's **toolbar** (§12 R09) drives the **readonly entries calendar** (§12 R16) live, with **NO grouping control** — grouped breakdowns left this view entirely for Reports (§09 R02 / `tt report --by`, G11), so **`#el-by-seg` is absent** — yet **every toolbar query still carries the REQUIRED grouping key `by:'day'`** (`ListEntriesQuery.by`; issue #55: a query without it throws in core and the calendar silently shows everything, so the mock is strict about `by` exactly like production). The toolbar controls are present and discoverable: a **range-preset** segment (`#el-preset-seg`, default This week) whose **Custom…** range is a **pair of plain date fields** (`#el-range-from`/`#el-range-to` are `input[type="date"]`, **no time component**, **no `#el-range-apply`** button — applied **LIVE** once both dates are set, §09 R01 / G3), **client / project / tag** filters (`#el-client`/`#el-project`/`#el-tag`), a **billable** toggle (`#el-billable-seg`), and the free-text **search** box (`#search`). Hardened per the issue-#55 triage over a **multi-week, multi-client, mixed-billable fixture** (7 entries across this week / last week / last month; all-time billable 8.00h vs this-week 5.00h): **EACH control is driven in turn and the visible event COUNT + `#week-total` are asserted to move to the expected subset** — counts, not just pixels. Machine-scored facts: `#el-by-seg` absent; idle default = 7 events with the **week-bounded** 5.00h chip (never the all-time 8.00h, §12 R16); a "refactor" search narrows to the two **in-week** matches at 3.50h (last week's refactor match stays excluded — **range + search compose**) and clearing restores; presets move the subset (month 6/7.00h, last-week 1/2.00h, last-month 1/1.00h, today 3/3.00h, week 5/5.00h); the billable toggle (billable 4/5.00h, non-billable 1/0.00h), client (3/2.50h), project (1/2.00h) and tag (2/2.50h) filters each narrow count + total; the Custom plain-date pair drives a `listEntries` call carrying the raw `{ fromDate, toDate }` strings (never a derived instant) narrowing to 2/2.00h; and across the whole drive **zero `listEntries` calls reject** with **every query carrying `by:'day'`**. All controls are **inked (monochrome)**, never accented (§15 accent discipline — accent stays on the primary action / running state). | §12 R09, §12 R16, §09 R01, §17 R11, §18 | `entries-calendar.png`, `entries-search.png` |
| `LIVE_FILTER` | **A search / filter selection is reflected LIVE in BOTH the list AND the report total (§17 R11), and the idle total is WEEK-BOUNDED (§12 R16, issue #55 Part B).** Loading the multi-week fixture (7 entries; all-time billable **8.00h**), the idle `#week-total` reads the **current week's** billable sum (**5.00h**, by the `weekStart` setting) — never the all-time total. Typing a **`refactor`** search narrows the visible list to the two **in-week** refactor rows (last week's 'refactor planning' stays excluded — the query composes range + search) **and** moves `#week-total` from **5.00h → 3.50h**, the **selected range's billable sum** — the instant repaint comes off the in-memory snapshot via the pure `window.SU.deriveView` (mirror of `src/liveview.ts`) with **no `getState` reload during the keystroke**, then settles on the authoritative queried set. Clearing the search returns both the list (7 rows) and the total (5.00h) to the week-bounded default. The total is the core-owned `billableSeconds` summed billable-only, equal to what `tt report` produces for the same selection. Machine-scored facts: list 7→2→7 rows, report total 5.00h→3.50h→5.00h, `getState` unchanged across the search keystroke, and **zero `listEntries` rejections** (the strict mock rejects a query missing the required `by`, exactly like core — issue #55); **and the no-match empty state** (STATES.md Entries × empty): a search matching nothing paints the query-narrowed **"No matching entries"** copy with the **"Widen the range…"** instruction — distinct from the never-tracked "No entries yet" copy — with zero rows and still zero rejections. | §17 R11, §12 R9, §12 R16 | `main-filtered.png`, `main-no-matching.png` |
| `CALENDAR_LAYOUT` | The Entries view content is the **readonly entries calendar** (§12 R16 / G10/G13/G16): **one day column per day in range**, laid over a shared hour gutter. The day columns are **equal width, never compressed below their comfortable 124px floor** — they share spare window width equally (§12 R22, the `WINDOW_GEOMETRY` half) — so when the range does not fit at the floor the strip **scrolls horizontally** (`.cstrip` scrollWidth > clientWidth). The vertical track is the **FULL 24h** (`.dt` ~24h tall) and the viewport **defaults to working hours** (`.cstrip` scrollTop lands on the working-hours-start offset, > 0) — a **scroll, never a clip**: an entry **before** working-start (06:00) and one **after** working-end (19:00) are both **in the DOM and reachable** by scrolling. Each **day header** (`.dh .ds`) shows that day's **billable total** (Mon 4.25h, Wed 1.00h) and the toolbar **range chip** (`#week-total`) shows the **week total** (9.25h) — and those headers are **on screen at the default paint, not merely in the DOM**: the `.dh` band and the `.gut` hour labels are **sticky**, so the working-hours scroll and the horizontal column scroll move the *content* past them instead of taking the **labels** away. An **empty day** renders as a **present-but-empty `.dcol`** (empty `.dt`, no events). **Hovering** an event reveals its **ops — Delete / Split / Edit — plus the corner `.ck` checkbox**; **clicking** an event body **opens the unified editor in the view-level host** (`#entry-form-host .edit-form.entry-form`, the event carrying the `.editing` selection state); the **running/open** block carries the **future-fade gradient with no end edge** (`.ev.run`); an **overlap** renders as a yellow `.ov` **warn band whose `.otag` names the amount** ("overlap Nm") and a **slept** span shows the `.zz` **hatched segment carrying the `#i-moon` marker** over its excluded portion (§12 R10 — the flag DETAIL + reversible subtract live in the editor, not on the calendar); and **checking two `.ck` boxes** reveals the **`#merge-bar` selection bar above the calendar** (§06 R3, design.html D11 / V5) — its count pill reading **"2 selected"** and its **Merge** a **neutral small button** (label "Merge", no `.primary`). Machine-scored facts: 7 equal floor-width columns + horizontal scroll at the default window; a 24h track whose default scroll lands on working hours with the off-hours entries present (never clipped); the per-day header totals + range chip; all seven `.dh` measured **inside the `.cstrip` scrollport** at the post-render scroll position, with the per-day totals **visibly** on screen and the hour labels with them, and both bands still there after the strip is scrolled fully right (the `.gut` left edge pinned to the scrollport's); an empty column; hover reveals ops + checkbox; a click opens the editor in `#entry-form-host` (the event marked `.editing`); the running block's future-fade; overlap `.ov` band whose `.otag` matches `/overlap\s*\d+m/` + slept `.zz` hatch carrying `#i-moon`; and the two-checkbox selection bar above the calendar with the `2 selected` pill + non-primary Merge. Fails if columns compress below the floor or diverge, the viewport clips (an off-hours entry missing), a total/empty column regresses, **the header band or hour gutter scrolls off screen**, the Merge button turns accent, or the hover/click/merge wiring breaks. | §12 R16, §06 R3–R4, §12 R10, §14 · design.html D11/V5 | `main-calendar.png` |
| `WINDOW_GEOMETRY` | **The app uses the window it is given, and every surface fits the window it ships in (§12 R22, issue #126) — outcomes, not controls, at real geometry.** Four machine-scored fact groups: **(a) the columns share the window** — over the one-week fixture, resizing 1040→1920 grows the seven `.dcol`s equally above their 124px floor until **all 7 days are fully visible with NO horizontal scroll**, and every description that ellipsised at the 1040 default renders its **full natural width** at 1920; **(b) the floor and the sanctioned scroll survive** — over the three-week fixture the range still doesn't fit, so columns hold the 124px floor and the strip still scrolls horizontally (§12 R16), while the wider window still pays out (more days fully visible at 1920 than at 1040); **(c) the minimum window commits** — at 1040×800 (the default AND the minimum, `main.ts`) the unified add form with its exact-times expander open shows **Save entry fully inside the viewport with no scrolling**, and the expander's Start/Stop fields sit **inside their picker column** with **no document-level horizontal overflow** in BOTH form modes (the overflow transferred from issue #146: `#add-to` ran 14px past the window, 63px past its column); **(d) the popover fits itself** — at the auto-sized window (`popoverWindowSize` over the measured card — the same clamp `main.ts` applies on show, so harness and app cannot drift) the card, the Stop/Start toggle, and Open Stint are all **fully inside the window** with nothing to scroll. Fails if a width cap returns anywhere (the week stops absorbing the resize), columns compress below the floor or stop growing, a truncated description stays truncated at 1920, the commit button falls below the fold at the minimum, the times row escapes its column, or the popover clips its card or either action. | §12 R22, §12 R16 | `calendar-wide.png`, `min-window-add.png`, `popover-fit.png` |
| `CALENDAR_ACCENT_BUDGET` | **The Entries calendar spends the accent budget on signal, not wallpaper (design.html D11 · §02 principles 1–2), machine-scored over a REALISTICALLY DENSE view.** The blocks used to fill `--accent-weak` behind an accent border, so the busiest screen in the app painted 51 copies of "the one thing that matters" and no primary action at all (issue #143). Driven at the 1040×800 default window over the three-week `denseCalendarState` fixture — 51 blocks, the last one OPEN — **at rest**, with no toolbar control touched: **(a) density** — ≥50 `.ev` blocks paint, so the measurement is the audit's condition and not a toy one; **(b) no wallpaper** — not one CLOSED block paints an accent-family colour (`--accent` / `--accent-solid` / `--accent-weak`) as fill, gradient, border or shadow, the rgba re-encodings of the retired `color-mix` border included; **(c) neutral surface + elevation** — every closed block computes the `--paper` fill and a **non-none box-shadow**, i.e. paper lifted off the track rather than a tinted box (§02 principle 2, "depth, not tint"); **(d) the signal survives** — **exactly one** block, the **running** one, still carries the accent (its future-fade gradient), because §02 principle 1 rations the accent for the primary action and the live running state; and **(e) the accent-solid budget** — at most **one** `--accent-solid` fill visible in the whole view (zero today; the budget is what promoting **Add entry** to the view's primary spends). Re-tinting the blocks — an accent-weak fill, an accent border, an accent hover wash — flips the pass to false. | design.html D11/D09 · §12 R16 | `calendar-accent-budget.png` |
| `SELECTION_LIFT` | **A chosen calendar block LIFTS; it does not turn accent (design.html D12), machine-scored over a REALISTICALLY DENSE view.** D12 says "a chosen thing lifts — a raised paper chip with a shadow (the segmented idiom) — it does not turn accent", and the Entries calendar broke it at all three of its selection sites: a merge-selected block took an accent border plus an inset accent hairline, the hover-corner checkbox that made the choice filled solid accent, and the block open in the editor (`.editing`) took the same accent ring (issue #144). Driven at the 1040×800 default window over the three-week `denseCalendarState` fixture the accent-budget guard uses — 51 blocks, the last one OPEN — because "lifted" only means something measured against the neighbours that are not: two blocks are selected through their `.ck` boxes and a third is opened in the unified form. Machine-scored facts: **(a) the chosen lift** — every `.on` and `.editing` block keeps the `--paper` fill and computes a box-shadow DIFFERENT from the resting rung its untouched neighbours carry (a real rung up the D09 ladder, read off the live surface rather than named, so the comparison stays token-free); **(b) the unchosen stay put** — the resting shadow is non-none, so the contrast the lift depends on exists; **(c) no accent on selection** — not one selected block and not one checked checkbox paints an accent-family colour (`--accent` / `--accent-solid` / `--accent-weak`) as fill, gradient, border or shadow, rgba re-encodings included, and the checked box computes the `--paper` fill (it is a paper box with an ink tick, not an accent chip); **(d) editing is the same idiom** — the third block opens `#entry-form-host .edit-form.entry-form` and carries `.editing`, lifting exactly like `.on`; **(e) the budget holds** — with three blocks in a selection state the calendar strip still paints ZERO `--accent-weak` fills and ≤1 `--accent-solid` (D11), so selection cannot smuggle the accent back onto the surface `CALENDAR_ACCENT_BUDGET` cleared. Re-accenting any selection site flips the pass to false. | design.html D12/D09/D11 · §12 R16, §06 R3 | `selection-lift.png`, `selection-lift-editing.png` |
| `CALENDAR_ENTRY_BLOCK` | **A calendar event contains its own content, and hovering one moves nothing.** A block's height comes from its **duration** (0.733px/min, floored at 18px) while its text height is fixed by **text flow** (~55px), so the two cross at about **75 minutes** — every shorter entry has more content than block. The block **clips** (design.html D09 — a region is signalled by its surface, so text outside that surface belongs to nothing): a short entry shows the description first and drops client/project then the time label as it shortens — **deliberate truncation** (design.html §4), never a spill into the hour rows below, which belong to other entries. The hover ops chip is a pure **overlay**: it reserves no flow space at rest and claims none on hover, so **no layout shift on hover** (design.html §4) — the checklist's own "reserve the space" remedy is refused here on purpose, since an 18px block cannot spare 36px of padding and reserving would only trade one defect for the other. Driven over a fixture seeded with the **four durations the design audit measured — 10 / 30 / 60 / 180 minutes** (a scene seeded with hour-plus entries alone reproduces the audit's own retracted kill, which was taken on a 132px block; a 30-minute standup is the common case). Containment is asserted by **hit-testing**, not by comparing layout rects — `overflow: hidden` clips paint, not layout, and the real defect was in the clip chain (the first non-visible-overflow ancestor was `.cstrip`, three levels up). Machine-scored facts: every block hit-tests to **itself** at its own centre (the probes are live, not off-screen); **no** point just below a block (+4px, +12px in its own column) hit-tests **into that entry**; the 10/30/60-minute blocks each genuinely **lay out more content than they have height for** (the fixture-realism guard — reseeding the scene with comfortable entries reddens this rather than quietly greening the scene); the 180-minute **control** block shows all three lines fully inside it (truncation only where the duration demands it); and the same event's title measured **hovered and not hovered** moves **exactly 0px** (the audit's 763 → 795, +32px measurement, kept as the guard) — measured against the **calendar strip's own scroll content**, not the viewport, because the driver's hover scrolls its target into view when it has to and a viewport reading books that scroll as a layout shift (issue 161's viewport-vs-page measuring error, met a second time). | §12 R16 · design.html D09/§4 | `main-calendar-short.png` |
| `CALENDAR_KEYBOARD` | **The entry calendar is one tab stop per entry, and nothing it focuses is invisible.** This row carries the calendar's half of the A04 floor ("focus visible on every interactive element, **never fully obscured**"), where `KEYBOARD_FOCUS` carries the window's. Each entry block hides four controls behind hover — the merge checkbox and Delete / Split / Edit — and all four used to be **top-level tab stops**: on the design audit's three-week seed the Tab key walked **~200 of them** before reaching anything else in the view, and fifty of them were the merge checkbox, which had `:hover` / `:checked` / `.on` opacity clauses and **no focus clause at all** while `.op-btn` beside it already took its opacity from `.ev:focus-within` — and since `opacity` takes the outline with it, a focused checkbox painted **neither the control nor its ring**, A04 failed as completely as it can be failed. The repaired model is a **roving focus**: the **block** is the one tab stop (a focusable `role="group"` labelled with the entry it stands for), its controls are `tabindex="-1"` and are reached with **← / →** from the focused block, **Escape** returns to the block, **Enter** opens the unified editor (the keyboard twin of the click-anywhere-on-the-body affordance), and `.ev:focus-within` opens the whole set — so arriving at an entry is also how a keyboard user learns the controls are there. Driven over the same three-week `denseCalendarState` (51 blocks, the last one open) at the 1040×800 default window; **density is the guard** (the issue-#55 lesson — at one day of data the traversal cost is invisible and any stop model looks fine). Machine-scored facts: the fixture really is the ~200-stop calendar (**≥50 blocks holding ≥150 hover-revealed controls**, so reseeding it comfortable reddens the scene rather than greening it); a Tab-walk from the top of the window stops inside the calendar **exactly `blocks` times** and **every one of those stops is a block**, never a control inside one; the walk **leaves** the calendar for the controls after it; **every stop of the whole walk**, calendar or not, holds a **non-zero effective opacity** (its own multiplied up its ancestor chain) and a **non-`none` outline** at the moment it holds focus — `outline` is declared nowhere in `styles.css` but the one focus rule, which GOLD `design-guard.test.ts` pins, so a non-none outline under focus **is** the D13/A04 ring; and on a sample block real **← / →** presses walk its four controls in DOM order, each inside that block and each visible while focused, **Escape** lands back on the block, and **Tab** from a control leaves for the **next block**, never for a control. | §12 R14, R16 · design.html A04 | `calendar-keyboard-focus.png` |
| `SETTINGS_VIEW` | The in-window **Settings** view (routed from the left-nav) exposes an **editable control for every §14 setting** — the **rounding** toggle and **rounding-increment** picker, **week start**, **first check-in**, **check-in interval**, **global hotkey**, and **date/number format** — each wired to the same `setSetting` IPC `tt config set` uses (no new channel). Changing a control **persists immediately**: editing the date-format select fires `setSetting` with the matching key/value (`{key:'dateFormat', value:'iso'}`). The panel chrome is **monochrome** warm grays — the controls are inked, painting **neither accent-family colour** (`--accent`/`--accent-solid`, design.html D11 — the accent itself is unconditional design, not a setting), and **selection follows the D12 raised-chip idiom**: the chosen segment button (`.seg-btn.on`) computes a **paper** background + **ink** text + a non-none **chip-lift shadow** while its unchosen peers stay flat (transparent, no shadow) — segment selection never turns accent. Machine-scored facts: routing to Settings renders a control for all seven §14 settings (by their `data-key`), the date-format edit fires `setSetting({key:'dateFormat', value:'iso'})`, no accent-family colour paints in the panel chrome, and the chosen segment is the raised paper chip with flat peers. | §12 R11, §14 · design.html D11/D12 | `main-settings.png` |
| `HOTKEY_NO_TRAP` | **This row carries WCAG 2.2 §2.1.2 (no keyboard trap) over the one control that captures raw keystrokes** — the Settings **global-hotkey** field. A capture field binds a chord by **swallowing** the key, so the traversal keys need an explicit hatch ahead of the swallow or focus enters and never leaves: issue 135, where `preventDefault()` was the handler's first statement and the four controls after the field in DOM order (**Date & number format**, **Check now**, **Backup retention**, **Restore…**) became unreachable from the keyboard — including the backup-restore path. `KEYBOARD_FOCUS` cannot see this, because it walks the default view where the whole Settings section sits behind `[hidden]` and its controls are out of the tab order; this row walks focus from **inside** the field. **Tab** advances out of it and **Shift-Tab** retreats out of it, and **neither writes a setting** — the sharp end of the regression is that `toAccelerator` reads Shift-Tab as the chord `Shift+Tab`, so the unfixed field did not merely trap focus, it silently **rebound the global hotkey** on the way out. **Escape** releases the field and likewise binds nothing (the craft-checklist keyboard clause — Esc cancels the innermost thing, which for a capture field is the capture). **Capture itself stays intact**: the hatch is three keys wide, not a disarming — a real chord still persists as its Electron accelerator. Machine-scored facts: all four stranded controls render; the field shows an A04 ring as a computed **delta** against its own unfocused signature; Tab from the field lands on another control (never `<body>`); Shift-Tab leaves the field with the `setSetting` spy still untouched; Escape leaves it, also writing nothing; a Tab-walk starting in the field reaches **all four** stranded controls; and `Ctrl+Shift+J` in the field persists `{key:'globalHotkey', value:'CommandOrControl+Shift+J'}`. | §12 R11, §14 · design.html A04 · WCAG 2.2 §2.1.2 | `settings-hotkey-focus.png` |
| `TIMELINE_WINDOW` | The **§14 timeline-window settings** and the **one viewport derivation** they drive (G16). The Settings view's **Timeline** group (between Reporting and Check-ins, per the mockup) renders an editable control for **all four data-keys** — the paired strict-HH:MM **Working hours** inputs (`workingHoursStart` / `workingHoursEnd`, reading the STORED values, here the non-default 09:00–15:00), the **Picker window** segmented control (`pickerWindowMode`: Working hours / Around now), and the **Around** span select (`pickerAroundHours`, options 4/6/8/12 h) — each persisting over the **existing `setSetting` channel** `tt config set` uses (no new IPC, no new parity row). The **Around row is disabled** (row class `off`, select `disabled`) while the mode is `working_hours` — and the dim that says so rides on the **control**, never the row: a disabled control is design.html §07's recorded **A01 exemption**, while the `.set-k` label beside it is ordinary readable text with no exemption to claim, so the row's old `opacity: 0.5` composited that label to **3.19:1** under a 4.5:1 floor (issue 155). No token check could see it, because no token was wrong — `--ink` on `--paper` is 16:1 and the **ancestor** was what darkened it — so the label is scored **as rendered**, the way the audit measured it: its own colour composited through the **whole ancestor opacity chain**, against the first opaque surface beneath it, held to **≥4.5:1** with **zero dimmed ancestors**. Re-dimming the row (or anything above it) reddens this row; `design-guard.test.ts`'s token pairing keeps scoring the colours themselves. **Flipping the Picker-window segment fires `setSetting({key:'pickerWindowMode', value:'around_now'})`** and the re-render **enables** the Around select (and the around_now fixture paints it enabled with the stored span from first paint). **`SU.timelineWindow` is the single source of the window math** — §12 R15's picker and §12 R16's calendar consume it and must never re-derive it: evaluated in-page under the pinned JUDGE_NOW clock it returns **exactly 540–900 minutes** (09:00–15:00) for the working-hours fixture and **JUDGE_NOW ± 4h clamped to the 24h track** for the around_now/8 fixture (the calendar always passes mode `working_hours`, reproducing the working-hours window). Once §12 R15/R16 land, their scroll container (the `data-timeline-track` hook) must open as a **full-24h scrollable track** — `scrollHeight > clientHeight`, `scrollTop` matching the configured window: a **scroll default, never a clipped one**; until then that sub-fact reports *pending* and is re-verified in the post-wave AC pass. Machine-scored facts: all four data-keys render with the stored values; Around disabled/off under `working_hours` and enabled under `around_now`; the off row's **label sits under no dimmed ancestor and clears A01 as rendered**; the mode flip fires the exact setSetting payload; the two in-page `SU.timelineWindow` evaluations match; the consumer track, when present, is scrollable and positioned at the window. | §14, §12 R12, §12 R15, §12 R16 | `timeline-window.png` |
| `SOFTWARE_UPDATE` | The Settings → **Software Update** group (§19 R03/R04/R06, G3), driven over the GUI-only `window.stint.update` bridge (the same `getVersion` / `check` / `download` / `reveal` / `onUpdateProgress` shape production's preload exposes). **Version (R06):** the Current-version row prints the stamped `APP_VERSION` read over `update.getVersion()` — the value `tt --version` reports. **Check (R03):** a "Check now" button whose click calls `update.check()` and paints the verdict — an "Update available · \<newer version\>" result line plus the `.pill.new` linking the release. **Guided download (R04):** a "Download & install \<version\>" primary action whose click calls `update.download()`; the replayed progress frames drive a live progress bar, and the terminal `ready` frame flips the action to "Reveal installer" wired to `update.reveal()`; the numbered guided steps include the one-time Gatekeeper / first-launch approval beat (no Developer ID). **No-DB (R04):** the panel carries the "Updates never touch the database" note (the artifact downloads to a temp folder, never beside the data). Machine-scored facts: the version row reads the fixture's stamped version; Check now paints the available verdict + release link; Download & install drives the progress frames to the Reveal-installer terminal state and fires `update.reveal()`; the guided steps include the Gatekeeper beat and the no-DB note renders. **Error phase (STATES.md Settings × error, the `downloadError` fixture variant):** a **rejected** `update.download()` flips the guided panel to its **error phase** — head "Update download failed", an **announced** (`role=status`) `.update-result.err` reading exactly "**The update download failed.**", and the retry **Download & install** action back in place, enabled (no Reveal-installer) — the failure is worded in place, never swallowed or wedged. **Failed check (STATES.md Settings × error, the `checkFails` fixture variant; issue 138):** a check that cannot reach GitHub resolves the **error verdict**, and the result line reads the **shipping** `UPDATE_CHECK_FAILED` sentence — imported from the built `ipc.js`, not re-typed here, so a reworded or re-broken copy fails the item — announced (`role=status`), naming **no transport at all** (no `net::ERR_*` code: the button used to report `net::ERR_NAME_NOT_RESOLVED`), with **Check now** still enabled and no Download action offered. | §19 R03, §19 R04, §19 R06 | `main-software-update.png`, `main-software-update-error.png`, `main-software-update-check-error.png` |
| `BACKUPS_SECTION` | The Settings view's **Backups** group (§20 R04 / §17 R12) renders, off the `getState` snapshot, the user-facing surface for automatic backups + retention + restore — **not** merely the `listBackups`/`restoreBackup` IPC channels (which `PARITY_REACH` already covers). Routing to Settings paints a **"Last backup …" status** line (off `state.lastBackupUtc`) with a **"verified" pill**, a **retention picker** (`backupRetention`) reflecting the snapshot value and persisting a change over the same `setSetting` channel `tt config set backup_retention` uses, and a **restore list** painted from `window.stint.listBackups()` (one row per backup — name · time · size, parity with `tt backup ls`), each row offering a **Restore…** action. A Restore is **destructive**, so it goes through the **§12 R13 confirm gate**: the first click only **arms** the confirm (`restoreBackup` not yet called); only the explicit confirm fires `window.stint.restoreBackup({name})` — **exactly once**, with the chosen backup's name. The whole group is **monochrome** (the verified pill uses the calm run tokens; no accent — §15 discipline). Machine-scored facts: the Last-backup line + verified pill render; the retention picker reads 5 and a change fires `setSetting({key:'backupRetention', value:10})`; the restore list paints two rows each with a Restore…; the first Restore… click arms without restoring; the explicit confirm fires `restoreBackup` once with the row's name; no stray accent paints in the group. **Empty (STATES.md Settings × empty):** a never-backed-up launch (no `lastBackupUtc`, `listBackups` → `[]`) paints **both** instructive copies — "**No backups yet** — Stint backs up automatically on launch." and "**No backups to restore from yet.**" — with zero rows, no verified pill, and the retention picker still operable. | §20 R04, §17 R12, §12 R13, §15 | `main-backups.png`, `main-backups-empty.png` |
| `RECOVERY_NOTICE` | The Settings → Backups **corruption-recovery banner** (§20 R05 / §17 R12). On a launch where the database was recovered from a backup (`state.recoveryNotice` non-null), routing to Settings renders a **one-shot banner** that names **both** the backup it recovered from (`recoveredFrom`) **and** the quarantined `.corrupted` file it set aside (`quarantinedTo`) — so the user sees exactly what happened, not a silent recovery. The Backups group + a reachable **Restore…** still render alongside it. The banner uses the monochrome warn `--flag` notice chrome (no accent). Machine-scored facts: the recovery banner renders naming both `recoveredFrom` and `quarantinedTo`, and a Restore… stays reachable on the same surface. | §20 R05, §17 R12, §15 | `main-recovery.png` |
| `PARITY_REACH` | **The rendered window surfaces an affordance for every capability (§17 R8).** Nothing `tt` can do is unreachable from the GUI: the persistent left-nav **routes to each of the five views** (Timer / Entries / Clients / Reports / Settings) and **each view exposes its actions** (the timer's Start/Stop, the Entries control bar + per-row edit/split/merge/delete, the Clients create/rename/archive, the report builder + exports, the Settings controls). The static coverage claim is asserted in `gui/test/parity.test.ts` (every IPC channel maps to a real `tt` command, **and** — bidirectionally — every distinct `tt` capability is covered by a parity-matrix row, modulo an explicit GUI-absent allow-set) and `parity-matrix.json`; the behavioural claim is asserted by `features/parity.feature` running over **both** surfaces. Machine-scored sub-fact here: the injected `window.stint` (the same preload surface production exposes) provides a **callable method for every `CHANNELS` entry** (a channel with no method is a capability the renderer cannot invoke) and the nav routes to all five views. Subjective: each view's affordances read as discoverable, not buried. | §17 R8, §12 R3 | `parity-timer.png`, `parity-entries.png`, `parity-clients.png`, `parity-reports.png`, `parity-settings.png` |
| `FIELD_LABELS` | **The D13 floor (every field carries a VISIBLE label), machine-scored — and with it the A01 field-border exemption design.html §07 CONDITIONS on that label.** The app labelled its fields with placeholder text, so the only name disappeared the moment the user typed; on the start form's four attribute fields (`#start-desc` / `#start-client` / `#start-project` / `#start-tags`) there was no accessible name at all, and `#add-desc` / `#search` / `#rep-name` carried a placeholder plus an INVISIBLE `aria-label` — which names the control for a screen reader and for nobody else (issue 136). `#add-desc` was the sharpest case: the one unlabelled field in a form whose Client / Project / Tags siblings all carried a visible one, an inconsistency inside a single form, and the mockups (`edit-entry.html`, `reports.html`, `timer.html`) had drawn the labels all along — the app drifted from its own illustration. Driving the real renderer through **all five views** at the 940×960 window, each routed into the states that actually hold fields — the Timer **start-details disclosure** (idle-only, §12 R05), the Entries **unified add form + Custom range**, the Reports **builder + Custom range**, and **Settings** (the Clients view carries no field at rest) — every VISIBLE form control (`input` / `select` / `textarea`) is swept for **two facts**: **(a) named** — **every** control has a programmatic accessible name from something other than its `placeholder`/`title` (an `aria-label`, a `<label>`, or `aria-labelledby`), the fact the four start-* fields failed outright; and **(b) visibly named** — every **field** has a **persistent visible element** supplying that name, through one of the three idioms the app uses: a wrapping or `for`-associated **`<label>`** with visible text (the D13 idiom), **`aria-labelledby`** resolving to a visible element, or the **row-heading** idiom the control bars and settings rows use (a visible `.report-lab` before a `.report-row`, a `.set-k` inside a `.set-row`, with `aria-label` carrying the programmatic half). A **placeholder is not an idiom** — vanishing on the first keystroke is the defect — and neither is a bare `aria-label`. Fact (b) governs the population D13's rule describes — the bordered-box controls ("one border, one radius, one focus idiom"). A **checkbox is not one**: it is named by the word beside it, and the calendar's corner select box is an **icon-only affordance**, which design.html **D16/A02** govern instead — those owe an accessible name, not a visible label, so fact (a) covers them and fact (b) does not. Every row-heading-idiom control is **named in the justification** so that population stays reviewable, exactly as `TARGET_SIZE` names its spacing exceptions. Guard-the-guard: the sweep must find **≥20** visible fields, so a scene that has gone blind fails instead of passing on an empty set. Fails the moment a field is added with only a placeholder, an `aria-label` is swapped in for a visible label, or a label is deleted from a field that had one. The Settings **global-hotkey capture control** is a `[tabindex]` span rather than a form control and is covered by `HOTKEY_NO_TRAP`. | §12 R14, §12 R05, §12 R07, §12 R08 · design.html D13/A01 | `field-labels-timer.png`, `field-labels-entries.png`, `field-labels-reports.png` |
| `FIELD_CHROME` | **The D13 field grammar ("**One** border (`rule-strong`), **one** radius (8px), **one** focus idiom: accent border + the 3px ring") and the D07 grid under its padding, machine-scored over the DRIVEN DOM.** The toolbar search box painted **two nested fields** (issue 149): the border, radius and padding sat on the `.search` `<span>` **wrapping** the input, and nothing reset the input itself — so Chromium's UA chrome (a `2px inset` border, square corners, an off-grid `padding: 1px 2px`) rendered a **second, smaller field inside the designed one**, and D13's focus idiom landed on **that inner box**: a square accent rectangle floating inside a rounded grey one. `#search`'s 1px paddings were the **only off-grid padding values in the running app**. Nothing could catch it — GOLD `design-guard.test.ts` scores the 4px grid over the **authored** values in `styles.css`, and every authored value was on-grid; these came from the **UA stylesheet**, which a static scan of the source cannot see. That is the limit of the static half rather than a bug in it, and this row is where the case belongs: the **driven-DOM** half of design.html §08's split. `FIELD_LABELS` drives the same five views over the same field population and asks how each is **named**; this row asks what each **looks like**. Two facts. **(a) The sweep** — every **visible field** (the bordered-box controls D13 describes: text inputs, selects, textareas — never the checkboxes) across **all five views**, each routed into the state that holds its fields (the Timer **start-details disclosure**, the Entries **add form + Custom range**, the Reports **builder + Custom range**, **Settings**; the Clients view carries no field at rest and is swept anyway so one added there cannot slip in unswept), resolves to **one solid `--rule-strong` border of one width on all four sides** — and the **same width as every other field in the window**, so "one border" is scored as **agreement** rather than pinned to a px literal (a restyle that thickens every field's hairline stays legal; one field that disagrees, which is what a leaked UA border is, does not) — an **`--r1` radius on all four corners**, and **computed padding on the 4px grid**. Every value compared is resolved from the **token layer at probe time**, so this is a computed check of design.html's own rule and not the CSS pin process.html §02 forbids; the one literal is D07's grid itself. **No exemptions**: the app really does paint every one of its fields the same, so a field that differs is news. **(b) The focus idiom lands on the field** — a real **Tab** onto `#search` (never `.focus()`, so the `:focus-visible` is the one a keyboard user gets) resolves the **accent border on all four sides** and the **3px ring** on the element that **holds focus**, still carrying D13's 8px radius, with its wrapper painting **nothing**. Fact (b) is what a wrapper-styled field can never satisfy, and is why the fix moves the chrome onto the input rather than resetting the input and leaving the wrapper as the box. Guard-the-guard: the sweep must find **≥20** visible fields, so a scene that has gone blind fails instead of passing on an empty set. Paint transitions are switched off for the probe (D10 fades border-colour over 120ms, and a pinned clock freezes a fade wherever it started). | design.html D13/D07 · §12 R09 | `field-chrome-search-focus.png` |
| `TARGET_SIZE` | **The A03 floor (target size), machine-scored over the surfaces the undersized targets actually live on.** Sweeping every **visible interactive control** (buttons, links, form fields, tabbable elements, `[data-act]` affordances) across **nine surfaces** — the five views of the running main window, the Entries **add form** and **unified editor** (both with their inline picker and a tag chip), the Timer **start-details disclosure**, the **Reports builder**, and the tray popover — every target's bounding box is **≥24×24 CSS px** (inclusive: the ops chip's 24×24 button against a 0px gap is a pass), or it stands **≥24px clear of every other target**, or it is an **inline link** inside a line of text. Two rules fix WHAT is measured: a control nested inside another target measures with its parent, and a **checkbox/radio inside a `<label>` measures as the LABEL** — the whole label is what a pointer aims at, so the 13px native glyph is a paint detail. Issue 148 found eight targets under the floor and three of them (`.ck` at 16×16, the tag remover at 10×17, 31 picker day cells at 23.84px) with 0–2px of neighbour spacing to fall back on; the sweep missed all three because it **rounded** before comparing, because the remover was a `<b>` matching no interactive selector, and because the transient surfaces were never opened. So the scene also asserts the three are **present in the swept set** — a remover that regresses to bare prose fails here, not just at the keyboard. Zero unsanctioned undersized targets gates the pass, and every spacing-sanctioned control is named in the report (the exact-times disclosure) so the exception list stays reviewable. The **toolbar search field** was on that list until issue 149 moved its chrome onto the input; at its real 33px height it now clears the floor outright rather than leaning on spacing. | design.html A03 | `main-target-size.png`, `target-size-add-form.png` |
| `COLOUR_PAIRING` | **The A05 floor (colour never the sole signal, WCAG 1.4.1) via the D05 pairing rule, machine-scored.** Every semantic colour in the window carries a **word or icon beside it**: the **run-dot** renders beside the literal word **"running"**, and every marker is scored **as rendered, not merely present** — the Entries strip pairs by its **visible dot** (D05 takes a word *or* an icon, and the strip's word is the deliberately hidden one — one indicator per surface, not two), while the **Timer card carries both a visible word and a visible dot**, the run-dot's contrast exemption in design.html §07 being *conditional on that pairing*. Presence alone is what this scene used to score, and it passed a Timer card whose word and dot were both `display: none` for as long as issue #142 shipped — a card signalling running by a recoloured clock and nothing else, the one thing D05 forbids. **billable-ness is worded**, not colour-only (the running card's `billable` / `non-billable` badge); the calendar's **overlap band carries its worded `.otag`** ("overlap Nm") and the **slept hatch carries the `#i-moon` icon marker**; and the two message palettes both carry **sentences**, not bare colour — the **warn advisory** (overlap banner) is worded on the `--flag`/`--flag-bg` palette while the **err block** (a refused Stop's `#timer-warning`) is worded on the `--danger`/`--danger-weak` palette with the Entries mirror carrying `.error` (D15 — an advisory never reads as a block, and neither state rides on hue alone). Fails if any of these markers loses its word/icon while keeping its colour — including by hiding it rather than deleting it. | design.html D05/D15/A05 | `main-colour-pairing.png` |
| `DESKTOP_FEEL` | The window reads as a quiet, minimal desktop app that respects the system (system typography, light-mode Warm Paper surfaces, monochrome with one rationed accent) — not a busy or branded web page. | §15 · design.html | all screenshots |

## Sub-facts

Each row's deterministic sub-facts, named as the harness records them
(`packages/gui/judge/run-judge.mjs` — a scene records a `{name: boolean}` map, and `pass` is
every fact true). `packages/gui/test/judge-bind.test.ts` binds the two sets both directions, so
an assertion added or dropped fails until this list moves with it. The captured-not-scored rows
carry none.

| Item | Sub-facts |
|------|-----------|
| `EMPTY_STATE` | `namesHotkey`, `namesCliStart` |
| `NAV_SHELL` | `orderOk`, `defaultOk`, `routedOk`, `sidebarEveryView`, `fixedWidthOnResize`, `chipOk` |
| `KEYBOARD_FOCUS` | `emptyWalkComplete`, `emptyRingsVisible`, `emptyNoTrap`, `runningWalkComplete`, `runningRingsVisible`, `runningNoTrap` |
| `TRAY_COUNTUP` | `clockSeeded`, `countsUp` |
| `TRAY_POPOVER_SURFACE` | `runningOk`, `idleOk` |
| `POPOVER_REJECT` | `refusalAnnounced`, `reasonAlone`, `stillOperable`, `repeatable` |
| `IN_WINDOW_TIMER` | `cardOk`, `stripOk`, `idleOk` |
| `CROSS_VIEW_FRESHNESS` | `toolbarLatched`, `idleCard`, `flipsInPlace`, `countsUp` |
| `TIMER_VIEW` | `clockLive`, `clockRole`, `startOnlyStrip`, `saysRunning`, `discInFlow`, `startOnlyPicker`, `startSeededExactly`, `dragWritesLive`, `patchStartOnly`, `attrVsFlag` |
| `FUTURE_START_GUARD` | `refusalAnnounced`, `nothingWritten`, `noWedge`, `reasonAlone`, `correctionCommits`, `stoppable` |
| `FAVORITES_RAIL` | `railSeeded`, `channelsCallable`, `resumeFires`, `pinLands`, `renameLands`, `unpinLands`, `emptyInstructs` |
| `ACCENT_DISCIPLINE` | — |
| `ACCENT_SOLID_BUDGET` | `everyViewWithinBudget`, `timerSpendsExactlyOne`, `everyStandingViewSpendsIt` |
| `PRIMARY_HANDOFF` | `everyStateExactlyOneFill`, `allFourteenStatesWalked` |
| `CLICKABILITY` | — |
| `START_ATTRIBUTES` | `attributesSent`, `billableSent` |
| `START_FORM` | `formOk`, `idleLabelOk`, `billDefaultOk`, `runningOk` |
| `RUNNING_SINGLE_ACTION` | `oneDescriptionField`, `startPanelHidden`, `editOrStopRemain`, `noSwitch` |
| `UNIFIED_FORM_ADD` | `layoutOk`, `paintOk`, `liveUpdate`, `savePatch`, `formCloses`, `overlapBanner` |
| `UNIFIED_FORM_EXPANDER` | `collapsedOk`, `fieldsOk`, `reflectedOk`, `savedOk` |
| `UNIFIED_FORM` | `hostShared`, `clickOpens`, `seeded`, `pickerOk`, `footer`, `savePatch`, `deleteGate`, `overlapDetailOk`, `sleptOk`, `exactTimesOk` |
| `MULTILINE_DESC` | `isTextarea`, `keepsNewline`, `scrollsNotGrows` |
| `OVERLAP_BANNER` | `hiddenBefore`, `raisedOnOverlap`, `announced` |
| `SPLIT_AFFORDANCE` | `closedOnly`, `chipGeometry`, `rawTextField`, `splitsInsideSpan` |
| `INLINE_GATE_CONTAINMENT` | `contained`, `chromed`, `unoccluded` |
| `WRITE_REJECTION_FEEDBACK` | `editRejected`, `splitRejected`, `renameRejected`, `toggleRejected`, `copyOk` |
| `ADD_REFUSAL_PALETTE` | `refusalAnnounced`, `reasonAlone`, `refusalReadsDanger`, `advisoryChromeIntact`, `advisoryShown`, `advisoryReadsFlag`, `palettesDiffer` |
| `MERGE_CONFLICT` | `barGatedOnTwo`, `promptOffersChoices`, `nothingMergedYet`, `escapeCancels` |
| `MERGE_CHOICE_LIFT` | `shapeOk`, `liftOk`, `noAccentOk`, `dotOk`, `followsOk` |
| `MERGE_NOCONFLICT` | `noPrompts`, `mergesDirectly` |
| `MERGE_GAP` | `gapArmsConfirm`, `nothingMergedOnArm`, `confirmMergesWithGap` |
| `DELETE_CONFIRM` | `armShowsConfirm`, `nothingRemovedOnArm` |
| `CONFIRM_DELETE` | `armsWithoutRemoving`, `confirmRemovesOnce` |
| `CONFIRM_DESTRUCTIVE` | `presentBefore`, `armsWithoutRemoving`, `confirmRemovesOnce` |
| `CLIENTS_VIEW` | `viewSeeded`, `createsLand`, `noRaceOnMutate`, `emptyInstructs`, `focusOrderReads` |
| `CONFIRM_ARCHIVE` | `armsWithoutArchiving`, `confirmArchivesOnce` |
| `RESTORE_ARCHIVED` | `archivedHidden`, `toggleRevealsRestore`, `restoreReactivates` |
| `TAG_CHIPS` | `chipsOnEvents`, `editorSeedsChips`, `tagsPatchOk` |
| `REPORTS_VIEW` | `listOk`, `sidebarOk`, `accentOk`, `builderOk`, `customOk`, `editOk`, `runOk`, `preRunExportOk`, `exportOk`, `oneOpenOk`, `kebabOk`, `refusalOk`, `emptyOk` |
| `ENTRIES_CALENDAR` | `controlsOk`, `defaultOk`, `searchOk`, `presetsOk`, `billableOk`, `clientProjectOk`, `tagOk`, `customRangeOk`, `wireOk` |
| `CALENDAR_LAYOUT` | `columnsOk`, `neverClipOk`, `totalsOk`, `emptyOk`, `flagsOk`, `runOk`, `hoverOk`, `labelsOnScreenOk`, `crossMidnightOk`, `editorOpen`, `mergeHiddenBefore`, `mergeShown` |
| `WINDOW_GEOMETRY` | `weekOk`, `denseOk`, `addOk`, `editOk`, `popOk` |
| `CALENDAR_ACCENT_BUDGET` | `densityOk`, `noWallpaperOk`, `neutralOk`, `signalOk`, `budgetOk` |
| `SELECTION_LIFT` | `stateOk`, `liftOk`, `noAccentOk`, `checkOk`, `budgetOk` |
| `CALENDAR_ENTRY_BLOCK` | `liveOk`, `containedOk`, `shortfallReal`, `controlIntact`, `noHoverShift` |
| `CALENDAR_KEYBOARD` | `fixtureReal`, `oneStopPerBlock`, `traversable`, `noInvisibleStops`, `rovingOk`, `escapeOk`, `tabLeavesOk` |
| `LIVE_FILTER` | `listLiveOk`, `totalLiveOk`, `noReloadOnSearch`, `noMatchOk` |
| `SETTINGS_VIEW` | `allControlsPresent`, `accentDiscipline`, `segChipOk`, `editFiresSetSetting` |
| `HOTKEY_NO_TRAP` | `allPresent`, `focusLandsOnHotkey`, `ringDelta`, `escaped`, `retreated`, `released`, `reachedAll`, `captureWorks` |
| `TIMELINE_WINDOW` | `groupSeeded`, `aroundDisabledUndimmed`, `modeFlipEnablesAround`, `windowMath`, `consumerTrack`, `aroundFixturePaints` |
| `SOFTWARE_UPDATE` | `versionOk`, `checkOk`, `downloadOk`, `errorOk`, `checkFailureOk` |
| `BACKUPS_SECTION` | `groupSeeded`, `retentionEditFires`, `restoreGated`, `neverBackedUpEmpty` |
| `RECOVERY_NOTICE` | `bannerNamesBoth`, `restoreReachable` |
| `PARITY_REACH` | `everyChannelPresent`, `allRouted` |
| `FIELD_LABELS` | `sweepReal`, `everyFieldNamed`, `noPlaceholderOnly` |
| `FIELD_CHROME` | `sweepReal`, `oneFieldGrammar`, `oneBorderWidth`, `focusOk` |
| `TARGET_SIZE` | `sweepReal`, `noUndersizedTargets`, `noneAbsent` |
| `COLOUR_PAIRING` | `stripPairs`, `cardPairs`, `billableWorded`, `calendarMarkersWorded`, `warnWorded`, `errWorded` |
| `DESKTOP_FEEL` | — |

**Guardrail.** JUDGE is non-deterministic, so it is never the sole gate on anything
that changes a billable number. It owns presentation and discoverability; the
arithmetic underneath those screens is already nailed by PROP and GOLD.
