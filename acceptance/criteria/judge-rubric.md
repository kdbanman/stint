# JUDGE rubric — GUI presentation & discoverability

The JUDGE method (acceptance.html §09) scores the subjective and emergent GUI
qualities that resist assertion. An agent drives the **real renderer** through an
injected `window.stint` mock (`packages/gui/judge/`), captures screenshots + the
accessibility tree, and scores each item below PASS/FAIL with a one-line
justification citing the screenshot. Deterministic sub-facts are asserted by the
harness directly; the genuinely subjective items are scored by an LLM/human over the
captured screenshots. Any FAIL fails the suite; a sample of PASSes is spot-checked.

The harness writes `acceptance/evidence/judge-report.json` and the screenshots in
`acceptance/evidence/screenshots/`. Run it with `npm run judge`.

> Renderer windows run headless via the pre-installed Chromium (Electron's own
> binary is not fetched in this environment). The **tray icon's own title count-up**,
> a **real global-hotkey press**, and the **tray's left/right-click behavior**
> (single left-click → compact popover only; right-click → minimal Quit-only menu;
> the dropdown action menu is removed, §12 R01 / G8) have no host here and are
> confirmed under MANUAL on a real desktop session (acceptance.html §11, residual
> risk). JUDGE checks only the headless-provable half: that the popover carries every
> tray action (`TRAY_POPOVER_SURFACE`).

| Item | Claim (PASS when true) | PRD | Evidence |
|------|------------------------|-----|----------|
| `NAV_SHELL` | The main window presents a **persistent left-hand nav** with the five views — **Timer / Entries / Clients / Reports / Settings**, in that order — beside the routed view area. Exactly one item is active by default (**Entries**), and its view is the one shown; clicking a different nav item **routes** to that view (its section becomes the visible one) and **moves the active highlight** to it (the active item carries the system accent, §12 R13). Routing is client-side (no IPC). The rail reads as a quiet desktop shell, not a busy web page. **Two hardened G7 sub-facts (MODIFIED §12 R3):** `SIDEBAR_EVERY_VIEW` — routing to **each** of the five views keeps the `.shell .nav` rail visible (width>0, not hidden) in **all five**, with exactly one `.view` visible each time, so **no view escapes the shell**; and `FIXED_WIDTH_ON_RESIZE` — the rail's measured width is **byte-identical (168px)** across the 480/760/1200px viewports while the `.views` column width changes, proving the sidebar is a fixed-width rail on resize (resize lands on the content area). A regression (a view that hides the rail, or a rail that shrinks/grows on resize) flips the pass to false. | §12 R3, R13 | `main-nav.png`, `main-nav-wide.png` |
| `EMPTY_STATE` | The empty main window instructs rather than decorates — it names a concrete next action ("press Ctrl+Alt+T" **and** "run `tt start`"). | §12 R5 | `main-empty.png` |
| `KEYBOARD_FOCUS` | Every interactive control in the window is **reachable by Tab in reading order** and shows a **visible, accent-disciplined focus ring** (a neutral system-gray ring on ordinary controls — the report/ghost/action buttons, the popover Open/Stop-Start — and the **accent only on the primary action / running state**, never elsewhere). The window is fully operable from the keyboard: a Tab-walk from `<body>` advances through every visible control with the active element **never trapped on `<body>` or null** (no focus void / trap), and each focused control paints a **non-default ring** (a real `outline` or `box-shadow` under `:focus-visible`, not the UA `outline: none`). Driven on both the empty and the running main window. Machine-scored facts: the Tab-walk reaches every visible focusable control in each window, each shows a ring, and focus never sticks on `<body>`. | §12 R14, §14 | `main-focus.png` |
| `TRAY_COUNTUP` | The popover shows a single running timer counting up; between two captures the displayed elapsed increased by ~3s, not reset. (The tray icon's own title count-up is checked under MANUAL.) | §12 R1 | `popover-running-1.png`, `popover-running-2.png` |
| `TRAY_POPOVER_SURFACE` | The compact popover is the **SOLE tray action surface** — the **Stop/Start toggle + Open Stint** are reachable in the popover, with **no Switch affordance** (Switch is removed entirely — issue #34) and **no dropdown action menu** (the tray's single left-click opens this popover; the old 3-item Start/Stop + Open Stint dropdown is **removed**, G8). The tray's own click/right-click behavior has no host headless and is confirmed under **MANUAL** (a single left-click opens the popover only; a right-click yields at most a minimal Quit-only OS menu). Machine-scored facts (the headless-checkable half): driving the real popover renderer, the **running** snapshot exposes `#toggle` reading 'Stop' (`aria-pressed=true`) and `#open` ('Open Stint') present, with **no `#switch` element** in the popover (running OR idle); the **idle** snapshot has `#toggle` reading 'Start' and `#open` present — so every tray action lives in the popover (since the dropdown is gone, the popover must carry them all; fails the moment Stop/Start or Open Stint is absent, or any `#switch` element reappears — Start *is* the atomic stop-then-start, so no separate switch verb is needed). | §12 R01 | `popover-running.png`, `popover-tray-surface.png` |
| `IN_WINDOW_TIMER` | The **full Active-Timer card lives in the Timer view**, while the **Entries view keeps only a compact strip** of it. In the **Timer view** (reached by clicking the Timer nav item) the full **Active-Timer card** mirrors `tt status`: a **live per-second count-up** (advancing +3s across a pinned-clock step, not reset), a **running state** indicator, the running entry's **description** + **client/project** label, and the primary **Stop** action (Stop carries the accent) — with **no Switch control** (Switch is removed; starting a new entry from the Start form performs the atomic stop-then-start, issue #34); the card's accent is confined to the running clock + state indicator (mirroring the popover count) and the rest of the card chrome stays monochrome. On the **Entries view** the **compact `#timer-strip`** mirrors the running count-up + state + description and links to the Timer view, but carries **no full-panel Stop control**. Machine-scored facts: the Timer-view card is hosted inside the `data-view="timer"` section, its clock reads the derived count-up and advances +3s, its text carries the running description ('auth refactor') and client/project ('Client A / API'), and a Stop control is present and visible while running **with no `#timer-switch` element anywhere**; AND the Entries-view strip shows the running count-up ('01:24:07'), state ('running'), and description ('auth refactor') while exposing no `#timer-stop` (and no `#timer-switch`). Would fail if the full panel stayed on Entries, the card/strip placement regressed, or a `#timer-switch` reappeared. | §12 R04, §12 R14 | `timer-view.png`, `main-timer.png` |
| `TIMER_VIEW` | The **full Timer view (G5)** is the GUI core-entry surface. Routing to the Timer view (clicking the Timer nav item) renders the **live clock** reading the **derived count-up** (now − startUtc − excludedSeconds) — it **advances +3s** across the pinned-clock step, **not reset** — a **running/idle state** indicator, and the running entry's **description** ('auth refactor') + **client/project** ('Client A / API'). The **live-edit-running strip** is present (it edits the OPEN entry's attributes + start time **without stopping it**), carries the **"no stop" pill** + the **"End time not editable while running"** note, and exposes **no End-time input**; committing a change sends an **`edit` patch over IPC** that carries the **start-time / attributes but NEVER `endUtc`** (the row stays open, §05 R6). A visible **Stop** (accent) is present while running — with **no Switch control** (Switch is removed; the start-with-details form, which stays available while running, performs the atomic stop-then-start, issue #34) — and the accent is confined to the running clock/state + the single primary action. Machine-scored facts: the Timer-view clock reads '01:24:07' and advances +3s; the running state/description/client-project read correctly; the live-edit strip is present with the no-stop note and no `#le-end`; an edit fires with a patch object that has **no `endUtc` key** and carries a `startUtc`/`description` change; and Stop is visible with **no `#timer-switch`** present. Would fail if the count-up reset, the strip closed the row (an `endUtc` in the patch), the End field appeared, or a `#timer-switch` reappeared. | §12 R14 | `timer-view-full.png` |
| `FAVORITES_RAIL` | The Timer view's **pinned favorites rail** renders **one row per `FavoriteView`** (name + client/project/billable meta), each with a **one-click Resume** that fires `window.stint.startFavorite({name})` **exactly once**, plus a **Pin-as-favorite** affordance that fires `window.stint.pinFavorite` and a **kebab** exposing **rename / unpin** (`renameFavorite` / `unpinFavorite`). The **empty-favorites state** instructs ('pin a favorite' / mentions `tt fav`). The rail chrome is **monochrome** (accent reserved for the primary action), and `window.stint` exposes a **callable method for each of the five favorite channels** (`listFavorites` / `pinFavorite` / `renameFavorite` / `unpinFavorite` / `startFavorite`) — the PARITY_REACH extension for favorites, so the rail is the reachable GUI surface for the `tt fav` family (§17 R14). Machine-scored facts: three seeded rows (Standup / Deep work / Admin) each with a Resume + kebab; a Pin affordance present; clicking the first Resume fires `startFavorite` exactly once with `{name:'Standup'}`; all five favorite channels callable on `window.stint`; and the empty variant paints the instructive empty state ('pin' + 'tt fav'). | §05 R09, §05 R10, §12 R14, §17 R14 | `timer-favorites.png`, `timer-favorites-empty.png` |
| `ACCENT_DISCIPLINE` | The system accent appears on the running state / primary action **only**; the rest of the chrome is monochrome system grays. | §07, §15 | `main-running.png` |
| `CLICKABILITY` | **Every clickable text affordance carries the neutral button background** (a non-transparent fill or a visible border); **inert text never does**; the system **accent stays reserved for each view's single primary action** (§15 R-clickability / G10). Over the running main window the harness walks every clickable affordance — `button:not(.primary)`, `.nav-item`, `.nav-link`, `a[href]`, `[data-act]` — and asserts each carries a non-transparent background **or** a visible border (no bare-prose offenders), with the in-control sub-affordances whitelisted (the in-chip `.chip-x`, the `.set-toggle` knob, and anything nested in an already-bordered `.chip`/`.seg`/`.presets`, plus the native multi-select checkbox which draws its own UA affordance); known inert text nodes (`.wordmark`, `.day-head`, `.entry .desc`, `.entry .time`, `.summary`) carry **no** button-like pill fill; and **only** the sanctioned accent uses (`button.primary` / running state / `.nav-item.active`) carry the accent, with at least one primary action carrying it — so the accent is reserved for the primary action. **Fails the moment** a clickable affordance (e.g. a `button.ghost` reverting to `background:transparent`) reads as plain prose, or inert text gains a pill background, or a second element steals the accent. Machine-scored. | §15 | `main-clickability.png` |
| `FLAG_IN_CONTEXT` | The overlapped entry and the slept-through entry each show their flag **on the affected row** — not in a separate list. The overlapped row also carries the **detailed overlap banner** spelling out the amount and which neighbour ("Overlap: 30m with previous/next entry", §12 R9); the slept entry shows the one-tap subtract **and**, once trimmed, its raw duration **struck through** beside the trimmed billable duration (the trimmed value is what bills, the struck one shows what was cut). | §12 R4, §12 R9, §10 R5 | `main-flags.png` |
| `START_ATTRIBUTES` | The Start offers optional inline attribute fields (description, client, project, tags, billable) behind a disclosure — the primary Start stays one-tap — and submitting sends every attribute over the `start` IPC, never a parameterless start. The surface is the GUI **core-entry** surface and **lives in the Timer view** (§12 R5, now `core`): the harness routes to the Timer view before opening the disclosure. | §05 R1, §12 R5 | `main-start-form.png` |
| `START_FORM` | The **start surface** as a whole (the GUI **core-entry** surface, §12 R5 — now `core` — which **lives in the Timer view**, relocated from the Entries toolbar): the Start offers the inline **description / client / project / tags / billable** form (defaulting billable per the §08 client rule) and starts **immediately** over the `start` IPC. The form **stays available while a timer runs** (it no longer flips to a separate Switch affordance): submitting it starts a new entry, **atomically stopping the open one first** — a strictly richer "switch" with no dedicated verb (Switch is removed, issue #34; starting *is* switching, §05 R01). There is **no `#switch` element** on the surface in either run state. The harness routes to the Timer view before reading the form. Machine-scored facts: the opened idle form exposes all five attribute controls and the idle primary button reads `Start`; the running snapshot keeps the start-with-details form present (its five attribute controls still reachable) **with no `#switch` element**. | §12 R5 | `main-start-form.png`, `main-start-form-running.png` |
| `ADD_FORM` | The main window offers a discoverable manual-add form behind a disclosure with **explicit from/to times** and the same attributes `tt add` accepts (description, client/project, billable, tags); the Save action carries the accent (the form's only accent use). | §05 R5 | `add-form.png` |
| `MANUAL_ADD_FORM` | Backfilling a completed entry from the form whose span **overlaps** an existing entry is **warned, not blocked** (§06 R4): the form's Save sends the explicit from/to + description/client/project/tags/billable over the `add` IPC (parity with `tt add`), the entry **still saves** (the form closes), and the SAME non-blocking inline overlap banner (`#overlap-banner`, `role=status` + `aria-live=polite`, allowed-but-flagged wording) the other write paths raise appears — the overlap is advisory, not a block. | §12 R7, §06 R4 | `main-add-form.png` |
| `ADD_FORM_PICKER` | The manual-add form's **Start (`#add-from`) and End (`#add-to`) text fields each expose a calendar-icon affordance** (`#add-from-pick` / `#add-to-pick`) that opens the §12 R15 visual time-range picker, seeded from the form's current span; **committing a span in the picker writes back** into those text inputs (5-min-snapped local datetime-local values). **Text entry stays authoritative**: the `#add-from`/`#add-to` inputs remain present, the picker never bypasses them, and a Save after picker use still sends the explicit `fromLocal`/`toLocal` (the values now in the text fields) over the `add` IPC. Fails if either affordance is missing, the picker does not open from the add form, the committed span does not flow back into the text inputs, or Save stops sending the text-field values. | §12 R7, §12 R15, §06 R4 | `add-form-picker.png` |
| `TIME_RANGE_PICKER` | The **visual time-range picker** (§12 R15 / G9, `timepicker.js` / `window.STP`) opens from the manual-add form's `#add-from` calendar icon and presents a **month calendar** + a **single-day hour-line track** with the bound text inputs **echoed at the top**. The edited entry is a draggable **accent "me" rectangle**: dragging the **body** moves start+stop **together** (5-min snap — a known +30px body drag advances **both** `#add-from`/`#add-to` echoes by the snapped amount), and dragging the **bottom resize handle** moves **only the stop** (5-min snap). **Other entries render gray** (`.stp-block.other`) and an **overlapping span paints yellow** (`.stp-overlap`, **warn-only** — `pointer-events:none`, never blocks Apply). **Apply** writes the picked **local** values into the authoritative `#add-from`/`#add-to` text inputs and **closes** the popover (text stays authoritative). **Accent discipline holds with the picker open**: only the primary **Apply range** button and the **"me" rectangle / selected day** carry the accent; the rest is monochrome. Machine-scored facts: calendar + track + ≥1 hour line present and the echoes mirror the seeded span; body drag moves both ends by the snapped 5-min amount; resize moves the stop only; ≥1 gray other + ≥1 yellow (inert) overlap; Apply leaves the picked values in the text inputs and dismisses the popover; zero stray accent offenders. | §12 R15, §06 R4, §15 | `time-range-picker.png` |
| `EDIT_RUNNING` | The running entry is editable inline — its row exposes an Edit affordance that swaps into an in-place form seeded with the entry's description/start; saving sends a patch over the `edit` IPC that never carries `endUtc`, so the entry stays open (the row keeps its running indicator and is not stopped). | §05 R6 | `main-edit-running.png` |
| `EDIT_INLINE` | Any field of an entry is editable in-context: clicking Edit on a (closed) row swaps into an inline form — not a separate page — seeded from the entry with description, start, end, billable, and a client select; the form sits inside the entry row and the seeded values match the entry. | §06 R1, §05 R6 | `main-edit.png` |
| `INLINE_EDITOR` | The per-entry **kebab (⋯)** opens the **consolidated entry editor** — one modal dialog surfacing **every `tt`-editable field** in one place (description, client, project, start, end, tags, billable), the GUI counterpart to `tt edit`, plus a **Split at instant…** control and a reachable **Merge** affordance (the row multi-select feeding the Merge action). The editor resolves no names — the Client/Project selects carry the entity **id** — and sends its writes over the same `edit`/`split`/`merge`/`remove` IPC `tt` uses (no new channel). The dialog chrome is **monochrome** system grays; only the primary **Save** carries the accent (§15 accent discipline). Machine-scored facts: the dialog appears with a control for every tt-editable field, seeded from the entry (`description` = 'design review', client pre-selected), and the Split control + the merge wiring (`window.SE.mergeSelected`) are reachable. | §12 R6, §06 R1–R3 | `main-editor.png` |
| `DELETE_CONFIRM` | Delete is destructive, so it requires a confirm step: the first click swaps the button into an explicit "Confirm delete?" affordance with a Cancel — the entry is not removed until the explicit confirm tap. | §06 R1 | `main-edit.png` |
| `CONFIRM_DESTRUCTIVE` | **Destructive actions confirm before acting (§17 R11).** The §17 framing of the gate: a **single Delete click must not destroy the entry** — it surfaces the in-window confirm (`.confirm` with `confirm-delete` + `cancel-delete`) and the entry is **still present** after the arming click (`window.stint.remove` not called, `__REMOVE_CALLS__` empty). **Only the explicit confirm** removes it, after which the entry is **gone from the list** (the remove mock drops it from the snapshot, so the post-confirm reload reflects the real deletion). Machine-scored facts: entry present pre-confirm with zero remove calls, then absent post-confirm with exactly one `remove({ id: 20 })`. (Archive-when-referenced reuses the same gate once the Clients view's archive control lands.) | §17 R11, §12 R13 | `main-confirm.png` |
| `CONFIRM_DELETE` | **Destructive actions confirm in the window (§12 R13 — `core`, loss-protection: destructive-action confirmation per §03).** A **single stray Delete click must not destroy an entry**: clicking Delete surfaces the in-window confirm gate (the generic `.confirm` affordance with a `confirm-delete` + a `cancel-delete` control) and the instrumented `window.stint.remove` is **not** called by that first click (`__REMOVE_CALLS__` stays empty). **Only the explicit confirm** removes the entry, and it fires `remove` **exactly once**, carrying the entry id. (The same gate is the GUI counterpart to `tt rm`'s confirm / `--force`; archive-when-referenced reuses it once the Clients view (R10) lands.) Machine-scored facts: the confirm gate appears, `remove` calls after the arming click = 0, and exactly one `remove({ id })` fires on confirm. | §12 R13 | `main-confirm-delete.png` |
| `SPLIT_AFFORDANCE` | A **closed** entry exposes a discoverable Split control that opens an inline instant picker (defaulting to the span's midpoint) and calls the split capability over the `split` IPC with a UTC instant inside the span; the **open/running** entry exposes no Split control (only a bounded span can be cut). The renderer adds no arithmetic — core enforces the strictly-in-span rule. | §06 R2, §12 | `main-split.png` |
| `OVERLAP_BANNER` | A write that creates an overlap surfaces a **non-blocking inline banner at the moment of the edit** — not only the durable per-row flag — worded as allowed-but-flagged ("This entry overlaps … — allowed, but flagged in reports."). The banner is `role=status` + `aria-live=polite` (announced, accessibility-tree visible) and auto-clears on the next refresh that carries no warning; the write still commits (overlap is warned, not blocked). It uses the `--flag` tokens, never the accent. | §06 R4, §12 | `main-overlap-banner.png` |
| `MERGE_CONFLICT` | Selecting **two-plus contiguous closed entries** reveals a **Merge** action (hidden with fewer than two selected). Merging entries that **disagree** on client/project or billable raises an inline conflict prompt offering the distinct client/project choices **and** a billable choice **before** any merge commits — the user picks which value the merged entry keeps (descriptions are concatenated and tags unioned by core regardless). An **agreeing** selection merges directly, with no unnecessary prompt. The renderer resolves no names: it sends the winning entry's id (`winnerId`) + the chosen billable over the `merge` IPC, and the main process maps that to core's `MergeOptions`. | §06 R3, §12 R6 | `main-merge-conflict.png` |
| `MERGE_NOCONFLICT` | The agreeing-merge counterpart to `MERGE_CONFLICT`: selecting **two contiguous closed entries that AGREE** on client/project and billable and clicking **Merge** fires the merge **directly — with no conflict prompt** (there is nothing to resolve, so the user is not asked an unnecessary question, §06 R3). The renderer sends just the selected **ids** over the `merge` IPC (no `winnerId`, since no winner needs picking); core does the actual fold (descriptions concatenated, tags unioned). Machine-scored facts: no `.merge-conflict` prompt appears, `window.stint.merge` is called exactly once carrying the two ids, and the payload carries **no** `winnerId`. | §06 R3, §12 R6 | `main-merge-conflict.png` |
| `CLIENTS_VIEW` | The **Clients** nav view lists the active clients with their projects nested under their client, and offers create/rename/archive **in place** (an Add client control, a per-client Add project / Rename / Archive, and per-project Rename / Archive). Archived items drop out of the active list (archive hides from the active list but keeps history — the entry labels stay resolved, not copied). The view chrome is monochrome system grays — the accent stays on the primary action / running state only (§15 accent discipline). | §07, §12 | `main-clients.png` |
| `TAG_CHIPS` | An entry's tags appear **as chips on its own row** — not in a separate list — and the running entry's tags also show on the summary line. The chips are **monochrome** system grays (the accent stays on the primary action / running state only, §15). Each affected row offers an **in-context Edit tags affordance** that opens an inline chip editor (removable chips + an "add a tag…" input), whose commit sends the minimal `addTags`/`removeTags` over the same `edit` IPC `tt` uses. Deterministic: the fixture's open row carries 2 tags and its closed row 1, so exactly 3 chips paint across the rows. | §07, §12 | `main-tags.png` |
| `REPORTS_VIEW` | The **Reports** view is the in-shell, PRIMARY surface for **saved, named report definitions** (G11) — it replaces the retired standalone `report.html`, so the **sidebar is present** with **Reports active**. Routing to it paints a **list of saved-definition cards**, one per saved def, each showing its **name + a spec summary** (the stored range + group-by + filters + rounding) and **Run / Edit** affordances (+ a kebab → Rename / Delete). The single accent affordance in the whole view is the **+ New report** primary action (§15 / G10 accent discipline — the cards, the builder, and the run-output stay monochrome). **+ New report** (and a card's **Edit**, which opens populated via `showReport`) reveals the **inline builder** — a name input, a **range** segment (the five presets + **Custom…**), a **group-by** segment (Client / Project / Day / Tag), **client / project / tag** filters, a **billable** segment, and a **rounding** toggle + 6/10/15/30 increment picker — saving over `saveReport` / `editReport` (parity with `tt report save` / `tt report edit`). Clicking **Run** on a card resolves+runs the definition through core (`runReport`, parity with `tt report run`) and paints the **grouped run-output** with the **resolved-range header** and **overlap + unreviewed-sleep flags in context** on the affected rows (the overlap chip on Globex / Q3 Strategy, the unreviewed-sleep chip on Initech / Market research — reusing the `REPORT_SUMMARY` shape), **not** in a separate list, using the `--flag` tokens, never the accent. **Export CSV / Export JSON** in the run-output each drive a real `exportEntries` call carrying the **saved report's ref**, so the bytes match `tt report run <name> --csv|--json`. The renderer resolves no names (it sends client/project **ids**) and re-derives no range/grouping/rounding/total. Machine-scored facts: one card per saved def with name + spec summary + Run/Edit; the sidebar present with Reports active; only + New report accented; + New / Edit open the builder with every control (presets incl Custom, the four group-bys, filters, billable, rounding+increment); Edit re-opens the matching def populated with a Delete affordance; Run sends the card's name and paints the grouped totals with both flags on their affected rows (none outside the table) + the resolved-range header; Export CSV/JSON invoke `exportEntries` with `format: 'csv'`/`'json'` and the saved ref. | §12 R08, §09 R08, §09 R09, §15, §18 | `reports-list.png`, `reports-run.png` |
| `ENTRY_LIST_SEARCH` | The Entries view offers a **control bar** above the list — a **range preset** row (default This week) with a Custom from/to, a **Group by** segmented control (**Day / Client / Project / Tag**, default Day, matching the day-grouped default), **client / project / tag** filters, a **billable** toggle, and the free-text **search** box. Typing in the search box **narrows the visible list live** (only matching rows remain) via a real `window.stint.listEntries` query (parity with `tt list --search`), and switching the **Group by** control **regroups** the same set into the chosen buckets (Day → Client buckets the entries by client) with each group header showing the key + summed billable hours. The default state (Day, This week, no filters/search) renders exactly as the plain day-grouped load. All controls are **inked (monochrome)**, never accented (§15 accent discipline — accent stays on the primary action / running state). Deterministic: search narrows the visible list to the matching rows and Group-by changes the grouping; subjective: the controls read clearly and the live-filter feels instant. Machine-scored facts: the four group-by options present + default Day, the range/client/tag/search controls present, a "refactor" search narrows the rows to the two matching entries (non-matches excluded), and switching to Client regroups into the client buckets (Acme / Globex) with every row returning once the search is cleared. | §12 R9, §18 | `entries-grouped.png`, `entries-search.png` |
| `LIVE_FILTER` | **A search / filter / group selection is reflected LIVE in BOTH the list AND the report total (§17 R11).** Loading the multi-entry fixture (four billable rows, **5.00h** total), typing a **`refactor`** search in one keystroke **narrows the visible list** to the two refactor rows **and** moves `#week-total` from **5.00h → 3.50h** (the billable-only report sum for the surviving rows) — both recomputed from the **in-memory snapshot** via the pure `window.SU.deriveView` (mirror of `src/liveview.ts`), with **no `getState` reload during the keystroke**. Clearing the search returns both the list (4 rows) and the total (5.00h) to the full set. The total is the snapshot's core-owned `billableSeconds` summed billable-only, equal to what `tt report` produces for the same selection. Machine-scored facts: list 4→2→4 rows, report total 5.00h→3.50h→5.00h, and `getState` is unchanged across the search keystroke (the live view never round-trips). | §17 R11, §12 R9 | `main-filtered.png` |
| `SETTINGS_VIEW` | The in-window **Settings** view (routed from the left-nav) exposes an **editable control for every §14 setting** — the **rounding** toggle and **rounding-increment** picker, **week start**, **first check-in**, **check-in interval**, **global hotkey**, **accent usage**, and **date/number format** — each wired to the same `setSetting` IPC `tt config set` uses (no new channel). Changing a control **persists immediately**: editing the accent-usage select fires `setSetting` with the matching key/value (`{key:'accent', value:'monochrome'}`). The panel chrome is **monochrome** system grays — the controls are inked, not accented (the accent stays on the primary action / running state only, §15 accent discipline). Machine-scored facts: routing to Settings renders a control for all eight §14 settings (by their `data-key`), the accent-usage edit fires `setSetting({key:'accent', value:'monochrome'})`, and no stray accent paints in the panel chrome. | §12 R11, §14 | `main-settings.png` |
| `PARITY_REACH` | **The rendered window surfaces an affordance for every capability (§17 R8).** Nothing `tt` can do is unreachable from the GUI: the persistent left-nav **routes to each of the five views** (Timer / Entries / Clients / Reports / Settings) and **each view exposes its actions** (the timer's Start/Stop, the Entries control bar + per-row edit/split/merge/delete, the Clients create/rename/archive, the report builder + exports, the Settings controls). The static coverage claim is asserted in `gui/test/parity.test.ts` (every IPC channel maps to a real `tt` command, **and** — bidirectionally — every distinct `tt` capability is covered by a parity-matrix row, modulo an explicit GUI-absent allow-set) and `parity-matrix.json`; the behavioural claim is asserted by `features/parity.feature` running over **both** surfaces. Machine-scored sub-fact here: the injected `window.stint` (the same preload surface production exposes) provides a **callable method for every `CHANNELS` entry** (a channel with no method is a capability the renderer cannot invoke) and the nav routes to all five views. Subjective: each view's affordances read as discoverable, not buried. | §17 R8, §12 R3 | `parity-timer.png`, `parity-entries.png`, `parity-clients.png`, `parity-reports.png`, `parity-settings.png` |
| `DESKTOP_FEEL` | The window reads as a quiet, minimal desktop app that respects the system (system typography, light/dark following the OS, monochrome with restrained accent) — not a busy or branded web page. | §15 | all screenshots |

**Guardrail.** JUDGE is non-deterministic, so it is never the sole gate on anything
that changes a billable number. It owns presentation and discoverability; the
arithmetic underneath those screens is already nailed by PROP and GOLD.
