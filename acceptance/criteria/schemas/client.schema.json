{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://stint.local/schemas/client.json",
  "title": "tt client ls --json",
  "description": "A client — the top of the client/project hierarchy (PRD §07). `tt client ls --json` emits an array of these (active only by default, `--archived` includes archived rows).",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "name", "archived"],
    "additionalProperties": false,
    "properties": {
      "id": { "type": "integer" },
      "name": { "type": "string", "minLength": 1 },
      "archived": { "type": "boolean" }
    }
  }
}
