{
  "name": "@stint/core",
  "version": "1.0.0",
  "description": "Shared core for Stint: schema, state transitions, invariants, reporting, rounding. The single source of truth imported by both the tt CLI and the Electron GUI.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build"
  }
}
